---
tags: [home, plan, механик, мобильное, ТО, заявки, офлайн]
date: 2026-08-22
---

# План - мобильное приложение механика и замыкание процесса

Заказчик принял предыдущий пул работ. Дальше замыкаем **два процесса
целиком**:

1. график ТО → задача механику в телефоне → исполнение по регламенту →
   результат у прораба;
2. заявка диспетчера → задача механику → исполнение → результат у прораба.

Оба разорваны в одном месте: **у механика нет рабочего инструмента**. Всё
остальное звено живое.

Контекст: [[фронтенд перешёл ко мне - монорепозиторий и доработка за деньги]],
[[план работ - интеграция фронта и рефакторинг]]. Подробности закрытых
этапов 0 и 1 вынесены в
`archive/plans/план механика - подробности этапов 0 и 1.md`.

## Состояние

| Этап | Состояние |
|---|---|
| 0. Фундамент на бэкенде | **сделан целиком**, все шесть пунктов |
| 1. Мобильный каркас | **сделан**; очередь починена, снятие `kIsWeb` отложено до APK |
| 2. Заявки | **сделан**, руками щёлкался только частями |
| 3. ТО | **сделан**, проверен руками |
| 4. Лента прораба | **сделан**, проверен руками |
| 5. APK в CI | не начат |
| 6. Состояние работы по ТО | **сделан**, проверен руками |
| 7. Наполнение строки списка | **сделан**, проверен руками |
| 8. Окно прораба «Сейчас в работе» | **сделан**, 8.1–8.7 закрыты, проверен руками |
| 9. Карточка работы у прораба | **сделан**, 9.1–9.3 закрыты; 9.3 не закоммичен |

Миграции этапа 0: `c5f1a8b34e70` (`updated_at`), `d7a2e5c19b41` (чек-лист и
фото шагов), `e3b9d47c58a2` (отметка «проверил»), `f4c1e6a29b73` (мягкое
удаление). Этапа 6: `a8d3f5b27c14` (`paused_at`, `commentary`). Правки контракта — в
[[журнал - изменения контракта API для фронта]].

Гонка в очереди исходящих починена 19 августа
([[очередь механика теряла и дублировала действия - enqueue и flush без замка]]).
Снятие 21 сторожа `kIsWeb` отложено до APK: на вебе они ничего не ломают, а
проверить правку без Android-устройства нечем. Оболочка ни разу не работала на
устройстве.

## Кадры макета — понадобятся на этапах 2 и 3

Файл «КПЭК» (`bsgJW2RZLQlZbYUq3peqbD`), страница «Механик | Мобильная
версия» (`1825:41`), четырнадцать кадров 375×667:

| Кадр | Узел |
|---|---|
| Авторизация | `1826:547` |
| Список заявок | `1826:155` |
| Уведомления | `1826:270` |
| Объекты | `1826:210` |
| Личный кабинет | `1826:283` |
| Информация о ТО | `1826:296` |
| Информация о ТО (подробнее) | `1826:329` |
| Этапы ТО 1 / 2 / 3 | `1826:392`, `1826:445`, `1848:505` |
| Анимация 1–4 | `1826:527`, `1826:531`, `1826:535`, `1826:540` |

Как достать: [[макет берётся из локальной Figma, REST остаётся запасным путём]].
Работаем по правилу [[фронт правится по макету, а не по коду подрядчика]].

## Поток из кода подрядчика — как справочник

Само приложение (`frontend/els_mobile/`, 4280 строк, Dart 2.19, в git не
добавлено) выбрасываем: оно не войдёт даже в систему — стучится в снесённую
`/cp/sign-in/`. Но описанный в нём поток верен и годится как справочник:

- **заявки**: `GET /order/for-me` → тап переводит в статус 2 (Принято) →
  «В работу» (3) → до пяти фото через `POST /order-photo/{id}/`,
  комментарий → «Выполнил» (4) или «Проблемы» (5);
- **ТО**: `GET /object/by-mechanic/` → `GET /planned-to/by-object/{id}` →
  матрица двенадцати месяцев → чек-лист шагов → отметка шага с комментарием
  и фото → сохранение.

Разбор целиком, с перечнем того, что выбрасываем и почему, — в архиве.

## Что согласовано с заказчиком

Ответы на заданные вопросы, а не предположения.

**Код подрядчика вливаем в существующий `frontend/`**, отдельным
приложением не оставляем. Один `pubspec`, один клиент API, одна
авторизация, один стиль.

**ТО закрывает механик**, и это сразу считается выполненной работой. Прораб
видит свежие сданные работы лентой и гасит счётчик отметкой «проверил».
Приёмки, блокирующей зачёт, нет.

**Платформы: web + APK для Android, iOS не делаем.** Почти у всех механиков
Android. APK бесплатен: та же кодовая база, тот же CI, ни магазина, ни
ревью. iOS — $99 в год и ревью, вернёмся, только если появятся iPhone.
Запускать на iOS-симуляторе как на инструменте разработки это не запрещает.

**Офлайн нужен полный, включая списки** — связь на объектах рвётся.

**Push пока не делаем.** Вкладка «Уведомления» — список внутри приложения
со счётчиком. Firebase и токены устройств — отдельный этап.

**Начинаем с заявок**, потом ТО: поток заявки известен целиком и не требует
миграций, кольцо замкнётся быстрее.

**Проверяем локально через `make up`**, тестовые данные завожу сам. Прод не
трогаем.

**Цена, названная до начала и остающаяся в силе:** полный офлайн делает APK
основной поставкой, а web — запасным входом. На телефоне локальная база
надёжна; в браузере то же живёт в IndexedDB, и браузер вправе её вычистить
(Safari — после ~7 дней без открытия). Кодовая база одна, но полный офлайн
обещаем только для APK.

## Этапы 2–5

Правило прежнее: контракт не ломаем, старые пути остаются живыми с пометкой
`deprecated` —
[[монорепозиторий - код в одном репо, но связь только через REST]]. Каждое
изменение контракта пишем в
[[журнал - изменения контракта API для фронта]].

### Этап 2. Поток заявки — **сделан 19 августа**

Кадр `1826:155` при разборе оказался не про заявки, а про плановые ТО, и
потока заявки в макете нет вовсе. Состав экрана согласован с заказчиком:
четыре раздела — сейчас, планируется, сделано, серый архив, —
[[список работ механика - четыре раздела вместо кадра про ТО]].

Что сделано:

- **Бэкенд**: `GET /order/for-me` отдаёт заявку и исполнителю, и механику
  объекта — [[заявку ведут двое - исполнитель и механик объекта]]. Пять
  тестов, всего в бэкенде 758 зелёных.
- **Список работ** с разбивкой по разделам, авариями сверху, пометкой
  «наблюдаю» на чужой заявке.
- **Карточка заявки**: приём, «в работу», «выполнил», «проблема»,
  комментарий, фотографии. Всё через очередь, отметка ложится в локальную
  базу сразу.
- **Фотографии в офлайне**: очередь научена multipart, снимок сжимается на
  входе — [[фотография ждёт связи в очереди и сжимается на входе]].
- **Уведомления** считает телефон, красная точка на колокольчике —
  [[уведомления механика считает телефон, а не бэкенд]].
- **Удалённое остаётся** в локальной базе (30 записей), иначе архив на экране
  взять негде.

Не сделано намеренно: листалка по дням из кадра (у заявки нет срока, у ТО
срок — месяц), галерея принятых сервером снимков, снятие задачи с человека.

### Этап 4. Лента у прораба — **сделан 19 августа**

Шёл раньше ТО по решению от 19 августа: так кольцо «заявка диспетчера →
механик → прораб» замкнулось целиком и его можно было показать заказчику.

### Этап 3. Поток ТО — **сделан 21 августа**

Кадры «Информация о ТО» (`1826:296`), «Информация о ТО (подробнее)»
(`1826:329`), «Этапы ТО 1–3». Экран «Мои ТО», регламент по шагам, закрытие
акта. На бэкенде готово всё, включая кнопку закрытия: `PUT /act-fact/{id}/`
пишет `finished_at`, механик проходит по правам и области.

Ручки этапа 4 готовы: `GET /work/submitted`,
`/work/submitted/unreviewed-count`, `POST /work/{kind}/{work_id}/reviewed/`;
карточка на главной со счётчиком непросмотренного и отметкой «проверил».

Не сделано намеренно: имён прораба и механика на «Подробнее» нет (в ответе
только числа), ссылки на карточку объекта нет — экрана нет.

### Этап 6. Состояние работы по ТО — **сделан 21 августа**

Сверх исходного плана, по разговору с заказчиком. Механик приостанавливает
работу и сообщает о проблеме с причиной; начатое и приостановленное ТО стоит
первым в списке. Миграция `a8d3f5b27c14`.
[[пауза по ТО - это «Принято» при начатой работе, а не новый статус]],
[[механик завершает работу, а слово «акт» остаётся в базе]].

### Этап 7. Наполнение строки списка — **сделан 21 августа**

В строке регламент значком, адрес второй строкой, срок «до 31 августа» и
прогресс серым хвостом рядом с пилюлей состояния. Сверх плана — счёт времени
у открытой аварийной заявки. Бэкенд не трогали, `title` в `/act-fact/for-me`
приходил и раньше. [[строка списка - короткая пилюля и серый хвост]],
[[счёт времени у аварии идёт от заведения заявки, а не от звонка]].

### Этап 8. Окно прораба «Сейчас в работе» — **сделан 22 августа**

Пауза, проблема и причина уже лежат в базе, но прораб их не видит: ручка
`/work/submitted` отдаёт только закрытое. Нужна соседняя ручка по той же
форме (`UNION` двух веток в `crud_submitted_works.py`), старую не трогать.

Место, состав и границы согласованы с заказчиком 21 августа: раздел «Сейчас в
работе» сверху ленты сданных работ; в нём и начатые ТО, и заявки в статусе
«В работе». Строка без кнопки «Проверил» — проверять нечего: бейдж вида
работы, пилюля состояния, серый хвост с числами, исполнитель и причина
цитатой. Своего кадра в Figma у ленты нет, макет собран из приёмов соседних
экранов — так же, как сам экран
([[экран отчётов собирается из приёмов соседних кадров, своего макета у него нет]])
и по правилу деления из
[[строка списка - короткая пилюля и серый хвост]]. Страница макета:
`https://claude.ai/code/artifact/8cdaca5b-3a61-4778-8703-11528e112672`.
Вторая страница, 21 августа, — экран целиком со стыками двух лент, меню с
тремя числами и карточка работы:
`https://claude.ai/code/artifact/e04f4c07-d01e-45b0-8f36-0eeb04b98e72`.

Решения заказчика по макету:

- список сам перечитывается **раз в минуту**, пока экран открыт;
- «Проблема» здесь **красная**, в сданных работах остаётся жёлтым бейджем —
  расхождение осознанное: незакрытая проблема и есть повод для раздела;
- видимость **по своим участкам**, уже общего правила — ручка берёт область
  записи, а не чтения:
  [[текущие работы прораб видит по своим участкам, а не по всем]];
- в боковом меню **два числа рядом**: сколько в работе и сколько проблем;
  вместе с круглым бейджем непросмотренного их на строке три — развели формой,
  таблетки против круга, нули не показываются;
- **строку открывают нажатием целиком**, а не стрелкой; у сданной работы всё
  остаётся как было, там кнопка «Проверил».

Работа поделена на маленькие этапы — за сессию берём один-два:

| Этап | Что в нём |
|---|---|
| 8.1 этап ручки — ветка ТО | `GET /work/in-progress`, пока одна ветка: начатые и незакрытые акты. Состояние собирается из статуса и дат (`3` — идёт, `2` при заполненном `started_at` — пауза, `5` — проблема), `since`, `reason` из `commentary`, `progress` из чек-листа, `title` регламента. Схемы, отбор по участкам, тесты |
| 8.2 этап ручки — ветка заявок | Заявки в статусе `3`, `UNION` с веткой ТО, порядок «проблемы → паузы → идущие», `total` и `problems` в ответе, `make openapi-update` |
| 8.3 этап раздела в ленте | Модель, репозиторий, блок раздела. Отдельный запрос со своими состояниями: сбой раздела не роняет сданные работы. Пустой раздел не прячется |
| 8.4 этап строки текущей работы | Вёрстка строки: бейдж, пилюля, хвост, исполнитель, причина. Узкая и широкая раскладки, как у строки сданных |
| 8.5 этап живого времени | Счёт в пилюле тикает раз в минуту и гаснет в `dispose` — тем же приёмом, что `_StatePill` у механика |
| 8.6 этап авто-обновления списка | Перезапрос раз в минуту, пока экран открыт; на неактивном экране опрос стоит |
| 8.7 этап счётчика в меню | Два числа рядом со «Сданными работами» в боковом меню, поверх существующего бейджа непросмотренного |

Не делаем в этапе 8, намеренно:

- **страниц нет** — одновременно ведут единицы работ; при переполнении список
  показывает двадцать и подпись «и ещё N»;
- **неначатых работ в разделе нет** — назначенное ТО это план, а план живёт в
  графике ТО;
- **у проблемы нет счёта времени** — момент, когда механик её объявил, в базе
  не хранится, а `updated_at` меняется от любой правки.

Пункт «строка ничего не открывает» **отменён 21 августа**: карточка работы
делается, но отдельным этапом 9.

### Этап 9. Карточка работы у прораба — **сделан 22 августа**

Строка раздела открывается нажатием в любое место и показывает карточку:
шапка повторяет строку, дальше чек-лист с комментариями и снимками внутри
пунктов, времена, исполнитель. Единственное действие — «Позвонить»; работой
распоряжается механик. Решение и его цена:
[[карточка текущей работы у прораба всё-таки нужна]].

Новых ручек не нужно: `GET /act-fact/{id}/`, `GET /act-fact/{id}/photos/` и
карточка заявки уже в проде. Макет — вторая страница записи по прорабу:
`https://claude.ai/code/artifact/e04f4c07-d01e-45b0-8f36-0eeb04b98e72`.
Там же экран целиком со стыками двух лент и меню с тремя числами.

| Этап | Что в нём |
|---|---|
| 9.1 карточка ТО | Экран карточки, чек-лист с обрывом на шестом пункте и «и ещё N», времена, звонок либо пометка «Номер не указан» со ссылкой в карточку сотрудника |
| 9.2 карточка заявки | Без чек-листа: задание или «Задание не описано», категория, время заведения, фотографии |
| 9.3 края карточки | Работу сдали при открытой карточке, сбой подробностей при живой шапке, чек-лист без снимков |

Карточка живая: перечитывает работу тем же минутным тактом, что и раздел, и
о конце работы узнаёт из своего ответа —
[[карточка узнаёт о конце работы из своего ответа, а не от раздела]].

Правило пустых полей общее для всей карточки:
[[незаполненное поле называется словами, а не прячется]].

### Этап 5. Сборка APK в CI — **не начат**

По принципу [[образы собирает CI, а сервер только скачивает]]: артефакт
собирает CI, механикам раздаём ссылкой. Упирается в состояние `android/` —
см. [[текущие приоритеты]].

## Грабли, которые уже стоили времени

0. **`pub get` в контейнере без тома под кеш** висел час при 21 секунде
   процессорного времени. Гонять с `-v flutter_pub_cache:/root/.pub-cache`,
   после прогона откатывать `frontend/pubspec.lock`.
0. **`make migrate` не довозит миграцию до контейнера** — у бэкенда нет
   bind-mount, код запечён в образ. Нужен `make release`.
1. **Сессия в тестах без автосброса** (`autoflush=False` в
   `src/session.py`). Правку модели доводить `db_session.flush()`, иначе
   фильтр в SQL считает по старому значению, а ответ — по новому, и тест
   врёт в обе стороны.
2. **`to_timestamp` теряет время суток**: `datetime` — наследник `date`, и
   первая ветка пересобирает дату из года, месяца и дня. Для новых меток
   брать `utc_to_timestamp`. Чинить старую функцию — **отдельное решение**,
   она поменяет даты во всех ответах сразу.
3. **Геттеры пишут посчитанные значения обратно в запись.** `getting_order`
   и `get_acts_facts` починены, но `get_object` и `get_universal_user`
   делают так же. С появлением `updated_at` это двигает метку синхронизации:
   простое чтение выглядит как правка. Трогая геттер — считать в переменные.
4. **`changed_since` не совмещать с `only_open`** ни в одной ручке:
   закрытая запись просто исчезнет из ответа, и телефон не узнает, что она
   закрылась.
5. **Новая ручка ломает снимок контракта** — это напоминание, а не поломка:
   `make openapi-update` и дифф в ревью.

## Чего в бэкенде ещё нет

Синхронизация построена только для заявок и ТО. Для полного офлайна
(«всё целиком, включая списки») не хватает `changed_since` у объектов и
справочников — колонка `updated_at` там уже есть, ручек нет.

За границей мягкого удаления осознанно осталось: **статистика и отчёты
архив считают** (включается в `src/core/archiving.py`), и **старые
сущности** — объект, компания, организация, договор, контактное лицо,
участок — фильтр по умолчанию не получили, это правка вместе с фронтом.

## Как запустить проверку

`make test-db-up` поднимает тестовую БД, `make test` гоняет всё, `make lint`
и `make format` — Ruff, `make openapi-update` пересобирает снимок контракта,
`make vault-check` проверяет имена файлов хранилища. Сейчас **753 теста**
зелёные, линт чистый.

Фронт проверяется локальным Flutter 3.47: `flutter analyze` (2583
замечания, ноль ошибок) и `flutter test test/mechanic` (19 тестов, два
красных — см. долги этапа 1).
