---
tags: [home, прод, выкат, авторизация]
date: 2026-08-19
---

# Выкат авторизации на прод - порядок, данные и откат

Выкат **не начат**: решение отложено 19 августа 2026. Порядок проверен по
коду 2026-08-13, миграция отрепетирована на копии боевой базы —
[[миграция репетируется откатом и повторным накатом на копии боевой базы]].

Пока выката нет, на проде живёт дыра с файлами: nginx отдаёт
`/api/v1/static/` сам, мимо бэкенда, и проверка доступа не работает — см.
[[текущие приоритеты]] и
[[nginx отдавал файлы мимо бэкенда и проверка доступа не работала]].

- [ ] **Отправить ветку и слить в `main`.** На удалёнке `b222ce7` — ветку
      прогнать через pull request, CI смотрит только `main` и `dev`. Пуш в
      `main` запускает `release.yml` с тегами `latest` и `sha-<хеш>`.
- [ ] **Задать в `.env` на сервере `SECRET_KEY`, `FIRST_SUPERUSER` и
      `FIRST_SUPERUSER_PASSWORD`** — без них бэкенд не стартует. `SECRET_KEY`
      потерял значение по умолчанию (`config.py:31`), `create_super_admin`
      вызван без `try` и падает на дефолтах. Пароль от 8 знаков.
      `SECRET_KEY` задать один раз и не менять.
- [ ] **Снять дамп базы** (`pg_dump`). Обратимо не всё: миграция сбрасывает
      пароли администраторам со слабыми паролями, старых хешей нет нигде.
- [ ] **Проверить данные**: нет пользователей с пустым email и нет email,
      совпадающих без учёта регистра — на обоих миграция останавливается.
- [ ] **Выкатить** — `make prod-deploy`. Фронт и бэк едут одним стеком.
- [ ] Предупредить, что все пользователи будут разлогинены.
- [ ] **Предупредить администраторов со слабыми паролями** — их `id`
      появятся в логе бэкенда предупреждением.

**Данные, а не код — сделать до выката:** три механика (id 55, 56, 73) не
назначены ни на один объект, клиент id 62 привязан к компании «Мега» без
объектов — все четверо увидят пустые экраны. Проверить, у всех ли прорабов
заполнен участок: без участка прораб не создаст объект (`403`, код `136`).

**Откат.** Миграция идёт одной транзакцией, при сбое база цела — чинить
данные и повторять. Если приложение ведёт себя не так после успешной
миграции: `alembic downgrade -1`, затем
`IMAGE_TAG=sha-<хеш прежнего коммита> make prod-deploy`. Крайняя мера —
восстановление из дампа с `DROP SCHEMA public CASCADE`.

Связано: [[текущие приоритеты]], [[план работ - интеграция фронта и рефакторинг]]
