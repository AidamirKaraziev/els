---
tags: [home, priorities]
date: 2026-08-11
---

# Текущие приоритеты

## Главная задача периода
Собрать проект в **монорепозиторий** (бэк + фронт Вовы) и довести фронтовую часть
до сдачи. Контекст: [[фронтенд перешёл ко мне - монорепозиторий и доработка за деньги]].
Пошагово: [[план работ - интеграция фронта и рефакторинг]].

## В работе
**Статистика на главной.** Заказчик хочет четыре виджета: просроченные ТО,
выполнение графиков, топ поломок, топ сотрудников. Делаем по одному, начали с
топа поломок. План на восемь этапов, закрыты 1, 3, 4 и 7 — см.
[[2026-08-11 - топ поломок от справочника до карточки на главной]].

Следующий шаг — **этап 6**: добавить необязательные `object_id`, `year`,
`month` в `/order/all`, чтобы клик по строке топа вёл к заявкам объекта за
месяц. Параметры именно необязательные, старое поведение не меняется.
Сразу за ним этап 8 — вместе они дают законченный сценарий: увидел «6
поломок» → ткнул → смотришь, какие именно.

## Срочное, не про код
**Домен оплачен до 20 октября и зарегистрирован на Вову как на физлицо.**
Просить надо смену администратора внутри Timeweb — регистратор и хостинг там
одна компания, переезжать к другому регистратору не нужно. Заодно выяснить,
кто платит в октябре. Если продления не будет, 20 ноября домен освободится
вместе со всей настройкой HTTPS.

## Осталось по топу поломок
- **Этап 2**: `done_at` у заявок не пишется из-за опечатки — добавить поле
  `done_at` в `OrderUpdate` и писать его в `crud_order.update_order` вместо
  `dane_at`, старое имя оставить принимаемым. См.
  [[done_at заявок никогда не заполнялся - опечатка dane_at в OrderUpdate]].
- **Этап 6**: фильтры `object_id`, `year`, `month` у `/order/all`.
- **Этап 8**: экран «Подробнее» — все колонки, фильтры по участку и клиенту,
  сортировка по заголовку таблицы, дельта к прошлому месяцу.
- **Этап 5**: выгрузка отчёта в PDF, переиспользуя регистрацию кириллического
  шрифта и `landscape(A4)` из `src/services/defective_act_pdf.py`.
  Excel решили не делать.
- Проверить прогон CI фронта на коммите `b4185c0`: собирается ли карточка и
  не заупрямился ли резолвер на новой прямой зависимости `http: ^0.13.6`.
  Локально Flutter SDK нет, синтаксис проверялся только парсером `dart`.
- Накатить миграцию `a1c7f3d92b40` на дев- и боевую БД (`alembic upgrade
  head`): колонки `fault_category.code`, `counts_as_breakdown` и индексы на
  `order`.

## Ждём ответа заказчика
- **Нужны ли нативные приложения или хватит веб-приложения?** Ответ решает,
  удалять ли `frontend/android` и `frontend/ios` (59 файлов) — вместе с ними
  исчезает блокер с Gradle. Спрашивать надо про две вещи: push-уведомления на
  iPhone (в вебе работают только для PWA на домашнем экране) и работу без сети
  в машинных отделениях.
- **Имя приложения и его идентификатор.** Нужны и для PWA-манифеста (сейчас там
  «els» и «A new Flutter project.»), и для сторов, если приложения останутся.
  Сейчас `applicationId` шаблонный — `com.example.els`, публиковаться с таким
  нельзя, а после публикации он не меняется.

## Осталось по фронту и монорепо
- **Адаптивность экранов** (40–80 ч) — класс `Responsive` есть, но используется
  в 5 файлах на главной. Большие экраны свёрстаны под десктоп, а в 64 файлах
  логика завязана на hover, которого на тач-экране нет.
- **PWA** — заполнить `web/manifest.json`: имя, иконки, цвет; снять
  `portrait-primary`, иначе табличная админка на планшете заблокирована.
- **PWA-манифест** — теперь, когда HTTPS есть, service worker регистрируется
  и приложение можно ставить на домашний экран. Осталось заполнить
  `web/manifest.json`.
- **Почистить `pubspec.yaml`**: 12 неиспользуемых пакетов (`badges`,
  `gradient_borders`, `toggle_switch`, `prompt_dialog`, `auto_size_text`,
  `calendar_date_picker2`, `html`, `convert`, `equatable`, `bloc_concurrency`,
  `stream_transform`, `flutter_svg`) и дубль `location` с `geolocator`.
  Не зависит от решения по платформам.
- **`frontend/pubspec.lock` обновить локально** — в контейнере он не
  сохраняется и сейчас расходится с `pubspec.yaml`.
- **Если нативные остаются** — пересоздать `android/` шаблоном: Gradle 7.4 и
  AGP 7.1.2 не понимают Java 21, а разрешения объявлены внутри `<application>`,
  из-за чего Android их игнорирует и геолокация на телефоне не работала бы.
- Проверить на сервере, что деплой не завязан на старые пути: compose переехал
  в `infra/`, команда запуска теперь с `--project-directory .`.

## Хвосты по проду
- **Посмотреть глазами, что фотографии грузятся под HTTPS** — профиль и
  карточка объекта. Проверить может только человек со входом в систему.
- **Через сутки убедиться, что cron отработал**:
  `tail -5 /var/log/certbot-renew.log`, ожидаемая запись
  `Cert not yet due for renewal`.
- **HSTS включить после 9 ноября**, когда сертификат и продление отработают
  один цикл. Строка лежит закомментированной в `infra/nginx/https.conf`.
  Раньше нельзя: с ним перестанет работать откат на `http.conf`.
- **Том `static_data` растёт и не чистится** — фото заявок и актов плюс PDF.
  Три фото по 4 МБ на акт, сто актов в месяц — полтора гигабайта в месяц при
  диске в 15 ГБ. Смотреть `docker system df -v`, решать сжатием при загрузке
  или выносом в объектное хранилище.
- **`BACKEND_CORS_ORIGINS` записан JSON-массивом** как аварийная мера. После
  выката `d5eeef9` работает и форма через запятую, можно переписать
  по-человечески.

## Ранее в фокусе
- **Домен, HTTPS и выкат образами из CI** — закрыто 2026-08-11, девять
  коммитов `8c2a5ac..d5eeef9`. Проект живёт на `https://els23.ru`, сертификат
  до 9 ноября, продление в cron на 4:17. Образы собирает CI, сервер только
  скачивает. См. [[2026-08-11 - домен, HTTPS и выкат образами из CI]].
- **Фронт Вовы в монорепо и стек в docker** — закрыто 2026-08-11, восемь
  коммитов `50689bb..9426010`. `make up` поднимает всё, веб открывается на
  `localhost:8080`, API проксируется через nginx. См.
  [[2026-08-11 - монорепо, фронт Вовы и запуск всего стека в docker]].
- Этапы 0, 1, 5 плана интеграции закрыты, этап 2 начат: снимок контракта API
  на 157 ручек и тест на его дифф готовы, интеграционные тесты на критические
  потоки — нет.
- Спросить у Вовы, на каком Flutter SDK у него собиралось (сборка в контейнере
  на `cirruslabs/flutter:stable` проходит, так что вопрос уже не срочный).

## Дальше по плану
- Остальные три виджета главной: просроченные ТО, выполнение графиков, топ
  сотрудников. Запрос и фильтры переиспользуют
  [[статистика - агрегаты за период через FILTER и общий подзапрос]], выбор
  месяца — общий `MonthPicker`, вёрстка —
  [[виджеты главной - Expanded по LayoutBuilder, а не по ширине экрана]].
- Этап 2 плана интеграции: интеграционные тесты на критические потоки —
  авторизация и роли, объекты, наряды, акты, генерация PDF. Снимок контракта
  API уже есть.
- Этап 3: сверка 33 моделей с миграциями Alembic.
- Этап 4: авторизация на 25 открытых ручек, удаление через DELETE, CORS по списку.

## Долги/заметки
- Полный список техдолга с цифрами: [[состояние бэкенда на 2026-08-11 - метрики и техдолг]].
- Старая ручка `/order/statistics/top-breakdowns` помечена `deprecated`, но
  жива. Удалять только после того, как убедимся, что на ней никто не висит.
- Поддерживать vault, чтобы Claude быстро находил контекст и записывал решения.
- Зафиксировать доменную картину «акты/наряды/объекты/организации/пользователи/роли»
  отдельными утверждениями в `knowledge/` и связать с `atlas/`.
- `src/models/defective_acts.py`: уточнить требования к «строкам дефектов» —
  нужна ли отдельная таблица строк ведомости.
