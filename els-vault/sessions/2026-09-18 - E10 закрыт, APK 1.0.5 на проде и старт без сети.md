---
tags: [session, механик, релиз, apk]
date: 2026-09-18
---

# 2026-09-18 - E10 закрыт, APK 1.0.5 на проде и старт без сети

Эпик E10 «Мобильное приложение и релиз» закрыт 10/10 (S10 снят).

- Прод на `0df21f8`: S07–S09 (раздача APK с сайта, полоса «офлайн, N
  ждут» и плашка новой версии, Sentry) и правка S11.
- `apk-publish` дважды: 1.0.4+5, затем 1.0.5+6 — `https://els23.ru/app`.
- Аудит APK ↔ сайт на телефоне: логин, лента, шаги акта с фото, дефект,
  закрытие акта, офлайн-очередь — совпадает. Одно расхождение — без сети
  перезапуск показывал экран входа — починено в `008f25c`:
  [[без сети приложение открывало экран входа при живом refresh-токене]],
  [[телефон помнит почту и профиль, но не пароль]].
- S10 «подпись заказчика при закрытии ТО» снят из плана — заказчику не
  нужна. Если вернётся — задел в старом handoff: `ActFact.customer_signature`,
  ручка по образцу `act_fact_step_photo.py`, `OutboxAction` с файлом.
- Знание E10 разложено: [[APK механика собирает CI, а на сайт выкладывает publish-apk с ноутбука]],
  [[крэш-репорты APK уходят в Sentry только со сборки CI]]. План механика 2026-08-22 ушёл в `archive/plans/`.

Открыто после E10: Sentry для бэкенда; `merge.py land` удаляет ветку до
push и падает на непротолкнутом `origin/main` (обход — `git push`, потом
`git branch -D`); автотеста на офлайн-старт нет.

Проверки: `make lint` чист, `make test` 1076, `flutter test test/mechanic`
158, `flutter build web` ок.
