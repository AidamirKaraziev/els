---
tags: [sessions, E11, прод]
date: 2026-09-16
---

# 2026-09-16 — E11 закрыт, форма «Новая работа» на проде

Выкачен `2ba4d8c`: миграция `e9b7d4a06c32` с категориями, форма «Новая
работа» у админа и прораба. Пользователь глазами создал заявку с сайта.
По дороге два фикса, оба влиты и на проде:

- выбор объекта одним кликом —
  [[объект в форме не выбирался - список жил фокусом поля и исчезал до tap]];
- список объектов: название заголовком, прокрутка вместо «уточни запрос».

`make lint` чист, `make test` — 1058; `flutter test test/works` — 38.
Решение эпика:
[[новая работа заводится с сайта одной формой для админа и прораба, а ТО в ней нет]].

## Открыто после сессии

- 16 актов ТО без объекта в ленте —
  [[в ленте объект без названия - акты ТО пережили удаление объекта]];
  удалить или фильтровать — решает пользователь.
- Объект «Создаю Тест» (ул. Стасова 182) не находится в форме — скорее
  всего `is_actual = false`; запрос по `objects` на проде не выполнен.
- Снести `dispatcher/widgets/add_application.dart` после приёмки.
- Фото в форме и лента заказчику-исполнителю — после оплаты.
