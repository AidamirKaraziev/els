import 'package:flutter/material.dart';

class TestClassMy extends StatelessWidget {
  const TestClassMy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        width: 500,
        height: 500,
        color: Colors.blue,
      ),
    );
  }
}
