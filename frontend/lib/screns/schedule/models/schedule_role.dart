/// Чьими глазами открыт раздел «Графики».
///
/// Роль приходит параметром сверху, а не выясняется изнутри: оболочка
/// подрядчика держит для админа и прораба разные списки экранов и разные
/// глобальные переменные, и кто именно смотрит, знает только место
/// встраивания.
///
/// Лежит в моделях, а не рядом с лентой: экран объекта берёт её оттуда же,
/// а тянуть перечисление из чужого экрана — значит тянуть за ним весь его
/// файл с блоком и репозиторием.
enum ScheduleRole {
  admin,
  foreman,
}
