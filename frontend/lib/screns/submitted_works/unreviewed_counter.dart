import 'package:flutter/foundation.dart';

/// Сколько сданных работ прораб ещё не смотрел.
///
/// Одно число на всё приложение: его показывает кнопка меню, а меняет лента.
/// Держим его в `ValueNotifier`, а не в глобальной `var` со `StreamController`,
/// как сделано у подрядчика с `numberReports`: там число зашито константой и
/// никогда не обновляется, а перерисовку приходится звать руками через
/// `setState` в чужом виджете.
///
/// Ответ `POST /work/{kind}/{id}/reviewed/` уже несёт новый счётчик, поэтому
/// после отметки второй запрос не нужен — лента просто кладёт сюда число.
final ValueNotifier<int> unreviewedWorksCount = ValueNotifier<int>(0);
