## E03 · Единая навигация и лента работ

Цель: админ и прораб ходят по одному бургеру с одним порядком пунктов, а заявки и акты живут на одном экране «Работы» и меняют статус на месте, не пропадая.
Зачем: [[навигация и работы как одна сущность, а не три экрана подрядчика]]

- [x] S01 сверстать единый бургер по схеме разделов · front · design
      готово, когда: enum разделов (заголовок, иконка, роли, экран) и один `AppDrawer`; на фикстуре у админа и прораба порядок Главная · Объекты · График · Работы · Отчёты · Компании · Сотрудники · Выйти; «Выйти» с подтверждением; скриншоты обеих ролей утверждены глазами
- [ ] S02 перевести оболочки админа и прораба на go_router · front · после S01
      готово, когда: раздел выбирается маршрутом, а не `IntTest.index*`; при переходах пункты бургера не двигаются и не пропадают, активный подсвечен верно; на web адрес и «назад» работают; старые экраны открываются по старым пунктам без регресса
- [ ] S03 сверстать экран «Работы» на фикстуре · front · design
      готово, когда: одна лента заявок и актов с пилюлей статуса (новая · принята · в работе · сдана · проблема), чипсы по статусу и виду со счётчиками, поиск по объекту, «Архив» как фильтр; стиль как у экрана графика из E01; набросок утверждён глазами
- [ ] S04 отдать единую ленту работ одной ручкой · back
      готово, когда: `GET /work/feed` отдаёт заявки и акты во всех статусах с курсором и `updated_since`; `/order/all`, `/work/in-progress`, `/work/submitted` живы и помечены deprecated; тесты и `make lint` чисты
- [ ] S05 подключить ленту к API и сделать её живой · front · после S03, S04
      готово, когда: bloc опрашивает `updated_since` каждые ~15 с, пока экран открыт; на `make up` механик берёт задачу в работу — у прораба карточка меняет пилюлю, не исчезая; фильтры и поиск идут через ручку
- [ ] S06 вывести топ сотрудников на главной прораба · front · после S02
      готово, когда: раздел «Главная» общий для обеих ролей, у прораба тот же топ сотрудников, что у админа, с теми же данными
- [ ] S07 показать бейдж новых работ на пункте «Работы» · front · после S05
      готово, когда: в бургере у «Работы» число новых/непросмотренных из `unreviewed-count`, обновляется вместе с лентой, при нуле скрыт
- [ ] S08 проверить на собранном стеке и снести замещённое · front+infra · после S07
      готово, когда: на `make up` пути админа и прораба пройдены по всем пунктам; удалены `my_drawer.dart`, `drawer_foreman.dart`, `IntTest.index*`, `screns/task/*`, `task_foreman/*`, `task_completed_*`, экраны архива задач; `dart analyze` чист, `flutter build web` собран
