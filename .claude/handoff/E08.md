---
этап: S05 — вид ТО заводится по-настоящему, «Создать шаблон» в мастере
статус: закрыт
дата: 2026-09-21
ветка: E08-S05
план: .claude/plan/E08-vidy-to-i-shablony.md
---

# Передача: S05 принят на живом стеке, остался S06 — выкатить на прод и проверить глазами

## Сделано и проверено

- Сценарий «Готово, когда» пройден руками на `make up` (8080, admin) 2026-09-21:
  «Добавить вид ТО» → `POST /type-acts/ 200`, вид в дропдауне без пометок →
  позиция 12 → «Сохранить» → `PUT /maintenance-program/by-model/11/ 200` →
  предпросмотр перезапрошен, Дек «нет шаблона», «Утвердить» погашена →
  «Создать шаблон» → редактор «Новый шаблон … · KLF.» → «Скопировать из…» →
  `POST /act-base/ 200` → плашка погасла → «Утвердить» → «Да» →
  `POST /planned-to/generate/ 200`, карточка объекта показала 12 ТО.
- 409: вид, заведённый мимо формы, введён в окне → `POST → 409`, под полем
  «Вид ТО с таким названием уже есть», окно открыто.
- Дубль из загруженного списка окно ловит само, без запроса (кнопка погашена).
- `make lint` зелёный; `flutter test test/schedule test/foreman` — 229 passed.
  `make test` (бэк) не гонялся — бэк в S05 не трогали.
- Локальная база после приёмки: виды `ТО S05` (14), `ТО S05 дубль` (15),
  `ТО S05 дубльновый`, шаблон на KLF., график объекта 90 на 2026 — тестовые,
  прод не тронут.

## Не доделано

- Замечено, не чинилось: мастер каждый раз шлёт `GET /planned-to/preview/`
  без `anchor_month` (422), потом повтор с `anchor_month=1` — два запроса
  вместо одного. Это штатный путь «начало цикла не определилось по 2025»,
  но 422 в сети выглядит как ошибка. Вне S05/S06; кандидат в следующий эпик.

## Следующий этап

**Цель.** S06 «выкатить на прод и проверить глазами» — E08 целиком на проде.

**Готово, когда.** На проде завели вид, шаблон на модель, программу и график;
механик в приложении видит акт с этими шагами; tests и lint зелёные.

## Первые шаги

1. Влить S05 в main (`/stage-start` сделает сам, либо `/stage-merge E08`);
   убедиться, что CI зелёный и образы собрались (в S05 ghcr.io падал по TLS —
   проверка: `curl -m 10 https://ghcr.io/v2/` должен дать 401).
2. Выкатить прод по обычной процедуре проекта (см. `infra/`, `make help`);
   миграций в E08 нет ли — проверить `backend/alembic` за S01–S02 перед выкатом.
3. На проде под админом повторить сценарий из «Сделано» на реальном объекте
   и модели, потом под механиком открыть акт этого ТО в мобильном приложении
   и сверить шаги с шаблоном.

## Не трогать

- Бэк `/act-base/*`, `/type-acts/*`, `/planned-to/*` — закрыты S01, S02.
- Экран «Шаблоны ТО» и мастер — приняты в S04, S05; правки только по багу,
  найденному на проде.

## Уточнить перед стартом

- Кто и когда выкатывает прод: окно, нужен ли бэкап БД перед релизом.
- На каком объекте/модели прода проверять — чтобы не завести лишний вид ТО
  в общий справочник клиента.

## Ссылки

- `frontend/lib/screns/schedule/object/wizard/view/schedule_wizard_screen.dart`
  — `_createTemplate`: откуда берутся модель и вид для редактора.
- `frontend/lib/screns/schedule/object/wizard/bloc/schedule_wizard_bloc.dart`
  — `_onTemplateSaved`: запись и перезапрос заготовки.
