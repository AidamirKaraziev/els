---
этап: S05 — вид ТО заводится по-настоящему, «Создать шаблон» в мастере
статус: в работе
дата: 2026-09-21
ветка: E08-S05
план: .claude/plan/E08-vidy-to-i-shablony.md
---

# Передача: код S05 готов и покрыт тестами, осталось прогнать сценарий на живом стеке руками

## Сделано и проверено

- Окно программы: «Добавить вид ТО» зовёт `POST /type-acts/` через
  `MaintenanceProgramRepository.createTypeAct` — вид приходит с `id` и сразу
  обычный; пометка «не в справочнике», жёлтая плашка и ветка `_hasUnknownAct`
  удалены, `TypeAct.id` больше не nullable. Отказ сервера (409 «имя занято»,
  обрыв) — красным текстом под полем, окно не закрывается —
  `frontend/lib/screns/schedule/object/wizard/widgets/wizard_program_dialog.dart`.
- Предпросмотр: «Создать шаблон» — по кнопке на каждый вид без шаблона (один
  вид — «Создать шаблон», несколько — «Создать шаблон: ТО 6»); открывает тот же
  `showTemplateEditorDialog` со «Скопировать из…», модель — из карточки объекта,
  вид — из клетки (`WizardPreviewCell.typeActId` ← `type_act_id`).
- Блок: `WizardTemplateSaved(typeActId, steps)` → `TemplatesRepository.save`
  с `modelId` мастера → перезапрос `GET /planned-to/preview/` с прежним якорем;
  ошибка — плашка, заготовка не теряется. `templatesRepository` — новый
  обязательный параметр `ScheduleWizardScreen`, `ScheduleObjectScreen`, блока;
  проброшен через opener, offer, dev-точки.
- `ApiTemplatesRepository.save` по мягко удалённой строке сначала зовёт
  `POST …/restore/`, потом `PUT` — иначе клетка «нет шаблона» не гасла бы
  (`available_type_act_ids` на бэке фильтрует `deleted_at IS NULL`).
- Тесты: +9 (диалог программы ×2, блок ×2, виджет мастера ×2, разбор
  `type_act_id`, restore-перед-PUT, новый `api_maintenance_program_repository_test.dart`);
  `flutter test test/schedule test/foreman` — 229 passed; `dart analyze` по
  затронутому чисто; `make lint` зелёный. `make test` (бэк) не гонялся — бэк не трогали.
- Глазами на фикстуре (`schedule-wizard-preview`, 5600): «Создать шаблон» →
  редактор «Новый шаблон ТО 6 · LIFT A388509» → «Скопировать из…» → сохранено,
  заготовка перезапрошена; «Добавить вид ТО» заводит вид и закрывается без плашки.

## Не доделано

- **Сценарий на живом стеке не проходили руками** — единственное, чего не
  хватает до `закрыт`. В сессии ghcr.io не отвечал (TLS timeout), `make up`
  образы не пересобрал; стек поднят из образов S04 (`docker compose up -d`,
  бэк не менялся), dev-точка `schedule-wizard-live` (5601, API → 8080) ждёт входа
  под админом — пароль вводит человек.
- В сборке 8080 мастер ещё старый (образ S04): пересобрать `make up`, когда ghcr.io оживёт.

## Следующий этап

**Цель.** Дозакрыть S05: прогнать сценарий на живом стеке и, если чисто,
пометить этап закрытым; дальше S06 «выкатить на прод и проверить глазами».

**Готово, когда.** Под админом на `make up`: «Добавить вид ТО» → вид в
позицию → «Сохранить» программу → в предпросмотре «нет шаблона» → «Создать
шаблон» → сохранить → плашка гаснет → «Утвердить» → график создан. В сети:
`POST /type-acts/`, `PUT /maintenance-program/by-model/{id}/`, `POST /act-base/`,
повторный `GET /planned-to/preview/`, `POST /planned-to/generate/`.

## Первые шаги

1. `make up` (ghcr.io должен отвечать: `curl -m 10 https://ghcr.io/v2/`); не
   отвечает — `docker compose -p lift_app --project-directory . -f infra/docker-compose.yml up -d`
   и dev-точка `schedule-wizard-live` из `.claude/launch.json` (порт 5601).
2. Прогнать сценарий из «Готово, когда» под админом; на 409 «имя занято» при
   «Добавить вид ТО» — проверить, что текст сервера виден под полем.
3. Чисто → `/stage-end` со статусом `закрыт` (галочка S05, ledger); нет —
   правка и снова сюда.

## Не трогать

- Бэк `/act-base/*`, `/type-acts/*`, `/planned-to/*` — закрыты S01, S02.
- Экран «Шаблоны ТО» (`templates/view`, `templates/widgets`) — принят в S04;
  в S05 тронут только репозиторий (restore перед PUT).

## Ссылки

- `frontend/lib/screns/schedule/object/wizard/view/schedule_wizard_screen.dart`
  — `_createTemplate`: откуда берутся модель и вид для редактора.
- `frontend/lib/screns/schedule/object/wizard/bloc/schedule_wizard_bloc.dart`
  — `_onTemplateSaved`: запись и перезапрос заготовки.
