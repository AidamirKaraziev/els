---
этап: E11·S01 — набросок формы «Новая работа» на утверждение
статус: закрыт
дата: 2026-09-16
ветка: E11-S01
план: .claude/plan/E11-novaya-rabota-s-sayta.md
---

# Передача: макет формы утверждён, дальше — живые ручки и сид категорий

## Сделано и проверено

- Кнопка «Новая работа» в шапке ленты «Работы» (`works_screen.dart`,
  параметр `onNewWork`; пусто — кнопки нет). Узкий экран — одна иконка.
- Диалог `screns/works/view/new_work_dialog.dart` на фикстуре
  `screns/works/repository/fixture_new_work.dart`, превью —
  `lib/dev/works_preview.dart` (launch `works-preview`, порт 5614).
  Пройдено в браузере: поиск объекта по нескольким словам, карточка
  объекта, «В работе сейчас», механик объекта подставлен исполнителем,
  выбор из групп по участкам + «Офис» + «Заказчики», категории сужаются
  по виду, создание кладёт строку в ленту, ошибка сервера строкой над
  кнопками, телефон — во весь экран. Утверждено пользователем глазами.
- `dart analyze lib/screns/works lib/dev/works_preview.dart` — чисто;
  `make lint` бэка зелёный. Тесты бэка не гонялись — бэк не менялся.
- Черновик `NewWorkDraft.toJson()` уже в форме `POST /order/`:
  `object_id`, `fault_category_id`, `executor_id` (может быть `null`),
  `task_text`.

## Не доделано

- Фото: в форме только место (`_pickPhoto` ставит имя файла). Выбор файла
  и отправка в `order_photo` — S02.
- Первый клик по строке списка после набора текста под автоматикой браузера
  «съедался»; руками не воспроизведено — пользователь смотрел сам.
  Если всплывёт на живой форме — смотреть `_ObjectSearch`, слушатель фокуса.

## Следующий этап

**Цель.** S02 — форма создаёт заявку через `POST /order/`, она в ленте.

**Готово, когда.** Админ и прораб создают заявку с сайта, она в ленте
«Работы», ошибка сервера показана. Область теперь `back+front+db`:
сид категорий и необязательный исполнитель — на бэке.

## Первые шаги

1. Бэк, миграция-сид `fault_category` (образец —
   `alembic/versions/2026_08_11_1200-a1c7f3d92b40_…py`): коды Р, НЛ, ЗЧ,
   О, ПР, М, УБ, ДОК, ДР («Другое») с `counts_as_breakdown=false`; имена —
   в `fixture_new_work.dart`. ТО (id 6) и Л (id 10) из формы не предлагать
   — на фронте фильтровать по коду, из базы не удалять.
2. Бэк: `OrderCreate.executor_id: Optional[int]` (`schemas/order.py:40`) и
   проверка в `crud_order.create_order` (`crud/crud_order.py:95`) только
   при непустом. Тест на создание без исполнителя.
3. Фронт: `NewWorkContext` собирать из ручек — объекты `GET /object/`
   (нужны `mechanic_id`, `foreman`, номера, участок, контакт), категории
   `GET /fault-category/all`, сотрудники — `feed.employees` + клиенты
   (сейчас ручка ленты клиентов не отдаёт — проверить, что расширить),
   открытые работы — лента с `object_id`. Отправка — `Api.post('/order/')`
   как в `dispatcher/widgets/add_application.dart:29`. После успеха —
   `WorksRequested()` и snackbar «Работа №N создана».
4. Кнопку показывать по праву `order:create` (админ, прораб).

## Не трогать

- Плановое ТО — только из графиков; в форме его нет намеренно.
- Старая форма диспетчера `add_application.dart` — не переписывать,
  удалить после того, как новая принята на проде (правило проекта).

## Уточнить перед стартом

- Клиенты в списке исполнителей: у клиента есть право видеть задачу в
  приложении? Если нет — задача «на заказчика» повиснет невидимой;
  решить, показывать ли ему ленту в этом же этапе или позже.

## Ссылки

- `.claude/plan/E11-novaya-rabota-s-sayta.md` — план эпика.
- `backend/src/services/work_kind.py` — почему вид работы вычисляется из
  категории и зачем флаг `counts_as_breakdown`.
