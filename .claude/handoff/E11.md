---
этап: E11·S03 — выкатить на прод и проверить глазами
статус: закрыт
дата: 2026-09-16
ветка: E11-S03
план: .claude/plan/E11-novaya-rabota-s-sayta.md
---

# Передача: форма «Новая работа» на проде, эпик E11 закрыт — дальше только хвосты

## Сделано и проверено

- Прод на `2ba4d8c` (`make prod-deploy`, миграция `e9b7d4a06c32` с
  категориями Р…ДР накатилась). Пользователь глазами под админом и
  прорабом: заявка с сайта создаётся, в ленте «Работы» видна.
- Выбор объекта одним кликом: список не привязан к фокусу поля
  (`new_work_dialog.dart`, `_ObjectSearch`) — раньше на вебе pointer-down
  снимал фокус, список исчезал до tap. Влито `ad5bca1`.
- Список объектов: название заголовком, адрес · тип · зав. № серым,
  прокрутка внутри рамки на ~6 строк, снизу «Всего N»/«Найдено N»; карточка
  выбранного объекта тоже ведёт с названия. Влито `2ba4d8c`.
- `make lint` чист, `make test` — 1058 зелёных; `flutter test test/works` —
  38, `dart analyze` чист.

## Не доделано

- 16 актов ТО с `object_id IS NULL` (№272–287 и рядом) висят в ленте как
  «Объект без названия · Принята · 337 д». Объект удалили, FK `SET NULL`.
  Решение не принято: удалить руками на проде (бэкап `pg_dump -t acts_fact`)
  или фильтр `object_id IS NOT NULL` в `_maintenance_branch`
  (`crud_work_feed.py`).
- Объект «Создаю Тест» (ул. Стасова 182) не находится в форме — скорее
  всего `is_actual = false` (форма отдаёт только живые,
  `get_new_work_objects`). Запрос по `objects` на проде не выполнен.
- Фото в форме: `_pickPhoto` ставит только имя файла; выбор файла и
  `POST /order-photo/{order_id}/` — отдельный этап.
- Заказчик в исполнителях: клиент задачу «на себя» видит, но показывать ему
  ленту — только после оплаты, по решению пользователя.

## Следующий этап

**Цель.** Эпик закрыт. Хвосты: удалить старую форму диспетчера
`dispatcher/widgets/add_application.dart` (правило: код подрядчика
удаляем после приёмки), решить судьбу 16 ТО-сирот.

**Готово, когда.** `add_application.dart` и её вызовы удалены, `flutter
build web` собирается, лента без «Объект без названия».

## Первые шаги

1. `grep -rn add_application frontend/lib` — точки входа старой формы.
2. На проде: `SELECT id, name, is_actual FROM objects WHERE name ILIKE
   '%тест%'` и `SELECT id, created_at::date, status_id FROM acts_fact WHERE
   object_id IS NULL` — цифры для решения.
3. Новый этап через `/roadmap` — либо в E10 (мобилка/релиз), либо новый эпик
   «хвосты формы».

## Не трогать

- Плановое ТО из формы — намеренно нет, только из графиков.
- `get_employees` ленты: заказчики только через `include_clients=True`.

## Ссылки

- `frontend/lib/screns/works/view/new_work_dialog.dart` — форма целиком.
- `backend/src/crud/crud_work_feed.py` — `get_new_work_objects`,
  `_maintenance_branch` (сироты ТО).
