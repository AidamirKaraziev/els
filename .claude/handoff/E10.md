---
этап: E10·S01 — выкатить прод: E02–E04 и карточка работы
статус: закрыт
дата: 2026-09-16
ветка: E10-S01
план: .claude/plan/E10-mobilnoe-i-reliz.md
---

# Передача: прод на main (`f6c66e5`), дальше — APK механика против прода

## Сделано и проверено

- Прод (`/var/www/els`, `make prod-deploy`) на образах `sha-f6c66e5…`: backend
  healthy, frontend 80/443, база на ревизии `d8a6c3f95b21 (head)` — миграции
  E02–E04 и карточки работы применены. Ошибок в `make prod-logs` нет.
- Пользователь глазами на проде: карточка работы из ленты «Работы», блок
  «Дефекты», PDF приказа о назначении из карточки объекта — работают.
- Кода в этапе не менялось; тесты не гонялись — нечего.
- Уведомления механика (задел для S04, по коду `mechanic/data/notifications.dart`,
  `mechanic_workspace.dart:51`): push нет ни на фронте, ни на бэке; приложение
  опрашивает сервер раз в 2 минуты и при `resumed`, разница → вкладка
  «Уведомления» и счётчик. Свёрнутое приложение ничего не получает; звука и
  вибрации нет. На устройстве пользователь ещё не проверял.

## Следующий этап

**Цель.** S02 — APK механика из CI работает на проде.

**Готово, когда.** APK логинится на прод, лента работ и дефекты видны;
расхождения записаны.

## Первые шаги

1. Взять APK из последнего зелёного прогона `frontend.yml` на main (`gh run
   list --workflow=frontend.yml --branch=main`), не из локальной сборки —
   ключ релиза только в CI.
2. Проверить, куда APK ходит: origin API в сборке (ветка `fix/apk-api-origin`
   в истории — там уже чинили) — должен быть прод, не dev.
3. Механиком (`m@mail.ru`) на устройстве: логин, лента заявок и ТО, открыть
   работу, посмотреть дефекты; расхождения с сайтом — списком в handoff для S03.

## Не трогать

- Код сайта: карточка объекта, поиск, блок «Документы» — замечания лежат в
  `inbox/E05.md`.
- Push/звук об аварийной заявке — сначала факт с устройства (S04), потом этап.

## Уточнить перед стартом

- Пользователь просит «новую задачу для админа и прораба» на сайте: бэк готов
  (`POST /order/`, право `order:create` у обеих ролей), нужна кнопка в ленте
  «Работы» + форма (образец полей — `dispatcher/widgets/add_application.dart`).
  Предложено отдельным эпиком E11, не в E10; решение за пользователем. Кадра
  Figma для формы нет — сначала набросок.

## Ссылки

- `.claude/plan/E10-mobilnoe-i-reliz.md` — план эпика, S01 закрыт.
- `frontend/lib/mechanic/data/mechanic_workspace.dart` — опрос раз в 2 минуты, точка для S04.
