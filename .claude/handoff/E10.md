---
этап: E10·S06 — релиз 1: push и очередь на проде
статус: закрыт
дата: 2026-09-17
ветка: E10-S06
план: .claude/plan/E10-mobilnoe-i-reliz.md
---

# Передача: прод на main с push и очередью, CI-APK проверен на телефоне

## Сделано и проверено

- Прод `els23.ru` на `3906f0c` (S03–S05): миграция `a4c1e7b9d2f0 (head)`,
  `/device-token/` в `https://els23.ru/api/v1/openapi.json`. Репо на сервере —
  `/var/www/els`, ssh-алиас `els` (root).
- Ключ Firebase лежит `/var/www/els/backend/firebase-service-account.json`
  (600), в `.env` прода `FIREBASE_SERVICE_ACCOUNT=firebase-service-account.json`.
  Внутри контейнера виден как файл `/app/firebase-service-account.json`.
- `docs/server-deploy.md`: §2.5 «Ключ Firebase», в §3.3 проверка push через
  `exec` (строка «Push включён» пишется при первом уведомлении, не на старте).
- CI-APK прогона `frontend` 35249858830 (sha256 `502d8fc8…1b8f`, ключ релиза)
  стоит на Xiaomi. Руками на проде: назначение/снятие заявки → push со
  звуком и иконкой, 3 раза подряд (S04); режим полёта → «в работу» + фото →
  «Очередь отправки» → сеть → пусто, фото на сайте (S05).
- `.claude/ledger.jsonl`: дубль строки S03 слит в одну с двумя хешами.
- `make lint` чист, `make test` 1076 зелёных.

## Не доделано

- В `/var/www/els` неотслеживаемые `els-1.0.0.apk`, `els-1.0.1.apk`,
  `release.json` — кто-то уже раздавал APK руками; это материал для S07.
- Первый push пришёл без звука: на телефоне громкость уведомлений (MIUI,
  отдельный от звонка ползунок) стояла 2/15. Приложение и канал `emergency`
  ни при чём — при жалобах механиков «push без звука» проверять это первым.
- `adb` не в PATH: `/opt/homebrew/share/android-commandlinetools/platform-tools/adb`.

## Следующий этап

**Цель.** S07 раздача APK с сайта.

**Готово, когда.** На сайте ссылка «Скачать APK» на последний релиз из CI и
`version.json` рядом.

## Первые шаги

1. Посмотреть, что уже есть: `ssh els 'cat /var/www/els/release.json; ls -l /var/www/els/*.apk'`
   и `backend/src/api/api_v1/endpoints/app_release.py` — есть ли он вообще.
2. Решить, откуда APK попадает на сервер: артефакт `els-apk` из
   `.github/workflows/frontend.yml` (14 дней) → шаг в CI или `scp` руками.
3. `infra/nginx/` — отдать `/app/els.apk` и `/app/version.json` статикой,
   ссылка на сайте.

## Не трогать

- «офлайн, N ждут» в шапке и плашка новой версии — S08.
- Sentry — S09.

## Уточнить перед стартом

- Кому виден APK: всем по прямой ссылке или только после входа на сайт.
- Откуда берётся `version.json` — из `pubspec.yaml` в CI или руками при релизе.

## Ссылки

- `docs/server-deploy.md` §3 — как выкатывается прод; §2.5 — ключ Firebase.
- `.github/workflows/frontend.yml` — где рождается APK и его sha256.
- `.claude/plan/E10-mobilnoe-i-reliz.md` — S07–S11.
