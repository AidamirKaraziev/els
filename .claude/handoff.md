---
этап: S5.4 — дефекты в карточке работы по ТО
статус: закрыт
дата: 2026-09-06
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: блок дефектов стоит в карточке ТО, ничего не закоммичено

Работа целиком в рабочей копии, коммитов в этой сессии не делалось. Текст
сообщения коммита выдан отдельно, коммитит человек руками. В срез правок
попала и передача прошлого этапа — `.claude/handoff.md` менялся до сессии.

## Сделано и проверено

Живьём на `make up` под прорабом, объект «Создаю Тест», работа ТО 1 за
сентябрь (`acts_fact` 296):

- **Блок «Дефекты · 2»** в карточке работы, между чек-листом и временами.
  Строка: заголовок, пилюля состояния справа, под ней приметы —
  `06.09.2026 · пункт чек-листа · снимков: 3`. Вида ТО в строках нет: он у
  всех один, это вид самой работы.
- **Клиентские потомки отсеяны.** В базе на этой работе четыре акта (6 и 7
  внутренние, 8 и 9 клиентские) — в блоке ровно два.
- **Строка ведёт в карточку акта** (`DefectCardScreen`), имя объекта
  передаётся из карточки работы: в самом акте его нет.
- **Дефектов нет — блок остаётся** со строкой «Дефектов на этой работе не
  заводили». Проверено на соседних работах (январь, ноябрь).
- **Отказ запроса не молчит**: «Дефекты не загрузились» и «Повторить» —
  в отличие от значка S5.3, который при неудаче пропадает. Живьём не
  воспроизводился, закрыт виджет-тестом.
- **Новая ручка `GET /defective-act/by-act-fact/{id}/`.** Выборки по работе
  не было, а `ActFactGet` дефектов не несёт и расширять её в проде нельзя.
  Доступ режется через `crud_acts_fact.get_act_fact_by_id`: чужая работа
  даёт 403, а не пустой список. Миграций нет.
- **Внешний вид утверждён на наброске** `lib/dev/work_defects_block_preview.dart`
  (точка запуска `work-defects-preview`, порт 5608): кадра в макете нет.

Проверки этой сессии: `make lint` чисто, `make test` — 978 (было 975),
`flutter test` — 489 (было 485), `flutter analyze` — 2390, как и до правок.
Снимок `backend/tests/snapshots/openapi_surface.json` обновлён — новая ручка
меняет поверхность API.

## Не доделано

Ничего по S5.4. Замечено попутно, в этап не бралось:

- «Месяц ТО» и «Год плана» в реквизитах дефекта — прочерки. Тянется с
  прошлой сессии.
- В списке объектов объект 82 показан как **«sss»**, хотя называется
  «Создаю Тест». Тоже с прошлой сессии.
- `CLAUDE.md` утверждает, что Flutter SDK локально нет. Он есть, 3.47.0.

## Следующий этап

**Цель.** S6.1 — сквозная приёмка дефектных актов на боевых данных.

**Готово, когда** пройдены четыре проверки из плана: дефект механика
доезжает после офлайна; прораб видит его в графике и в объекте и оформляет
клиенту, PDF с кириллицей; счётчик за прошлый год считает своё; график на
2027 создаётся мастером. Рядом лежит незакрытый S2.6: он требует чистого
`flutter analyze`, а там 2390 замечаний.

## Первые шаги

1. Открыть план, раздел S6.1 — четыре пункта чек-листа, идти по ним подряд.
2. Учётки и вход: `els-vault/knowledge/patterns/тестовые учётки локального
   стека - какая роль под каким логином.md`. Вход в браузере начинает
   человек, пароль агент не вводит.
3. Офлайн механика — очередь `frontend/lib/mechanic/data/outbox.dart`.

## Не трогать

- Ручку создания вида ТО; `status_id` и `PUT /defective-act/{id}/status/`.
- Легаси `els_mobile/`, `soon_screen.dart`, заглушки `SizedBox.shrink()`.
- `IntTest.indexScreensForeman` и гигантский `build` в
  `object_page_foreman.dart` — только добавлять.
- `ActFactGet` и ручку `/act-fact/{id}/`: их тянут другие экраны.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — разделы S6.1 и S2.6.
- `frontend/lib/foreman/defects/work_defects_section.dart` — образец того,
  как экран без блока ходит за дефектами сам.
