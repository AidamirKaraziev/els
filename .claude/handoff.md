---
этап: S6.2 — выкат дефектных актов и графиков на прод
статус: закрыт
дата: 2026-09-06
план: нет — план закрыт целиком, уехал в els-vault/archive/plans/
---

# Передача: план «окно график объекта» выкачен и закрыт, впереди APK

Ветка `graphs` слита в `main` (fast-forward, голова `46523d7`) и выкачена.
Кода не писали. Правки vault лежат в рабочей копии незакоммиченными, текст
коммита выдан — коммитит человек руками.

## Сделано и проверено

- **Прод перешёл с `a8d3f5b27c14` на `d8a6c3f95b21`** — три миграции:
  триграммы `b6e4a9d31c05`, программа ТО `c7f5b2e84a19`, дефектные акты
  `d8a6c3f95b21`. `make prod-deploy` прошёл, запускал человек.
- **Репетиция на копии боевой базы**: накат <1 с, `downgrade a8d3f5b27c14`
  возвращает схему точно к боевой, повторный накат чист, строки целы.
- **`pg_trgm` вопросом не оказался** — создаёт сама `b6e4a9d31c05` через
  `CREATE EXTENSION IF NOT EXISTS` под суперпользователем образа.
- **Боевой дефектный акт от 2026-04-07 пережил ALTER**: миграция дозаполнила
  ему `object_id = 44` обратным выводом через `planned_to`.
- **Проверка домена снаружи**: `GET /api/v1/maintenance-program/by-model/1/`
  отвечает 401, а не 404 — новый код бэкенда на месте; боевой PDF
  `defective_act/1/pdf/…` без токена 401; `../../.env` уходит в SPA-заглушку.
- **Проверки**: `make lint` чисто, `make test` 981, CI на `main` зелёный —
  139 тестов механика, release собрал образы.

## Не доделано

- **Цепочка под ролями на боевом домене глазами не пройдена** — вход
  человеком, пароль агент не вводит. Механик заводит дефект по ТО → прораб
  видит его в значке, ленте и карточке → оформляет клиенту и скачивает PDF
  с кириллицей → мастер раскладывает график на год.
- **`flutter analyze` не чист** — 972 замечания в CI. Это последний
  невыполненный пункт закрытого `S2.6`; ни от чего не зависит, берётся фоном.
- **Рабочая ветка не проверяется CI.** Джобы висят на `push: [main, dev]` и
  `pull_request`; 41 коммит `graphs` доехал до `main` без единого прогона.
- **Дамп боевой базы** — `scratchpad/els-prod-2026-09-06.sql.gz` вне репо:
  почты и хеши паролей. Удалить, когда точка отката станет не нужна.

## Следующий этап

**Цель.** Этап 5 плана механика — сборка APK механикам в CI.

**Готово, когда** CI отдаёт устанавливаемый APK, и он ставится на живой
телефон. Упирается не во фронт, а в состояние `frontend/android/`: Gradle
7.4, AGP 7.1.2, Kotlin 1.6.10, `namespace` не объявлен, плагины требуют
AGP 8. На машине нет ни SDK, ни Studio, ни Java, ни AVD.

## Первые шаги

1. Прочитать раздел «Открыто по этапу 1 и мобильной сборке» в
   `els-vault/00-home/текущие приоритеты.md` — там весь список по `android/`.
2. Открыть `.github/workflows/frontend.yml`: там же решаются триггеры (ветка
   или pull request) и версия Flutter под образ `cirruslabs/flutter:stable`
   (3.44.0) — сейчас зелёная джоба не говорит о прод-сборке ничего.

## Не трогать

- Прод без спроса: `make prod-deploy` и всё на `els23.ru` запускает человек.
- 21 сторож `if (kIsWeb)` вокруг выбора фото — снимать после APK, до него
  проверить нечем.
- Легаси `els_mobile/`, `soon_screen.dart`, заглушки `SizedBox.shrink()` в
  `_screens` — нумерация там позиционная.

## Уточнить перед стартом

- Домен оплачен до 20 октября и оформлен на Вову как на физлицо — кто платит
  и переносим ли администратора. Без продления 20 ноября уходит и HTTPS.
- Этап 5 берём целиком или сначала приводим `frontend/android/` в порядок
  отдельной работой?

## Ссылки

- `els-vault/00-home/текущие приоритеты.md` — читать первым, только открытое.
- `els-vault/sessions/2026-09-06 - дефектные акты и графики выкачены на
  прод.md` — как шёл выкат и что проверено.
- `els-vault/knowledge/debugging/работа на ветке доехала до main без
  единой проверки CI.md` — про триггеры, если браться за них.
