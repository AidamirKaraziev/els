---
этап: 5 — мастер расстановки графика (S2.4), набросок
статус: в работе
дата: 2026-09-02
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: набросок мастера написан, глазами не принят

**Ничего не закоммичено.** Работа лежит в рабочей копии поверх `118a420`.

## Сделано и проверено

- **Мастер из трёх шагов** — `screns/schedule/object/wizard/`: экран
  `view/schedule_wizard_screen.dart` (шаг в `setState`, блока нет), шаги в
  `widgets/` — программа модели, точка отсчёта, предпросмотр.
- **Модели повторяют `SchedulePreview` бэка** —
  `wizard/models/schedule_wizard_data.dart`: `position` — месяц цикла,
  `month` — месяц календаря. Когда мастер поедет на живую ручку, разбор
  ответа ляжет в эти же поля.
- **Занятый месяц не блокирует утверждение**: `WizardPreviewCell.mark`
  отдаёт `occupied` раньше `templateMissing` — расстановка такой месяц не
  трогает, и краснеть ему не за что.
- **Кнопка «Создать график на N» открывает мастер**, а не расставляет год
  молча (`schedule_object_screen.dart`, маршрут поверх как у карточки ТО).
  «Утвердить» закрывает мастер с `true`, и экран шлёт прежний
  `ScheduleObjectGenerateRequested` — старое поведение не сломано.
- **Проверки этой сессии**: `flutter test test/schedule/` — 90 passed (восемь
  новых в `test/schedule/object_schedule_wizard_test.dart`),
  `flutter analyze` по новым файлам и `schedule_object_screen.dart` — чисто.
  `make lint` и `make test` не гонялись: бэкенд не трогали.

## Не доделано

- **Глазами не смотрели — ни в браузере, ни на утверждении.** Чеклист
  приёмки выдан в терминале прошлой сессии, ответа по нему нет. **Пока вид
  не принят, логику не писать** — правило репозитория.
- **Выбранный на шаге 2 месяц никуда не уходит**: `generate` зовётся без
  `anchor_month`, как и раньше. Помечено комментарием в мастере.
- **Ручки `preview` и `generate` мастером не зовутся** — сознательно, весь
  третий шаг на фикстуре `wizard/fixture_schedule_wizard_data.dart`.
- **Правка программы модели не сделана** — решено этой сессией: шаг 1 только
  показывает 12 позиций. Сохранение — отдельной задачей.
- **Переход «Создать шаблон»** у клетки «нет шаблона» — заглушка: экрана
  шаблонов чек-листа ещё нет.
- **Кадра на мастер нет**, рисовали сами в стиле принятого экрана объекта.

## Следующий этап

**Цель.** Показать набросок, собрать правки, после утверждения — посадить
мастер на `GET /planned-to/preview/` и `POST /planned-to/`.

**Готово, когда.** Прораб проходит три шага на живых данных и график
появляется в базе; пока есть клетка «нет шаблона», «Утвердить» не нажимается.

## Первые шаги

1. Показать вид: `flutter run -t lib/dev/schedule_wizard_preview.dart -d
   chrome` — там переключатель трёх раскладов и флажок «есть прошлый год».
   Пройти чеклист, спросить правки.
2. После утверждения — репозиторий мастера рядом с
   `object/repository/schedule_object_repository.dart`: `preview(objectId,
   year, anchorMonth)` и `generate(...)`. Фикстуру не выкидывать, она держит
   тесты и dev-точку входа.
3. `anchor_month` с шага 2 передавать в обе ручки. Без него `preview` даёт
   422 (`num: 144`), когда графика за прошлый год нет.

## Не трогать

- Бэкенд графика: `preview`, `generate`, `object_id` у `/schedules/rows`.
- Старый `schedule_page.dart` и прочие экраны подрядчика — они в проде.
- Готовые блоки экрана объекта, лента месяцев и оба перехода — приняты
  2 сентября.
- Права и роли: `USER_READ`, `canOpenEmployeeCard`. Отдельная задача.
- Программу модели: `PUT /maintenance-program/by-model/{id}/` в этом этапе
  не зовём.

## Уточнить перед стартом

- Принят ли вид мастера по чеклисту и какие правки.
- Что делает ссылка «Создать шаблон» у клетки «нет шаблона»: ведёт на экран
  шаблонов чек-листа, которого пока нет, — или остаётся текстом до S3.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — `S2.4` и дальше.
- `backend/src/api/api_v1/endpoints/schedule_plan.py` — обе ручки и коды
  ошибок, на которые садится мастер.
- `~/els-figma/окно-график-объекта_1182-232.png` — кадр экрана объекта;
  на мастер кадра нет.
