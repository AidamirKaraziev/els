---
этап: 8 — макет прораба целиком (экран, меню, карточка)
статус: закрыт
дата: 2026-08-21
план: els-vault/00-home/план - мобильное приложение механика и замыкание процесса.md
---

# Передача: макет прораба дорисован и утверждён, дальше — код раздела в ленте

**Кода в этой сессии не написано ни строки.** Рабочая копия ровно та же, что
и в начале: незакоммиченная работа этапа 8.1 (пять новых файлов бэка, правка
`api.py`, снимок контракта) и заметки vault. Последний коммит — `2ec7807`.
Чужой `frontend/els_mobile/` — не наш.

## Сделано и проверено

- **Макет прораба нарисован целиком** — вторая страница записи по этапу 8
  (ссылка ниже): экран в двух раскладках со стыками двух лент, меню с тремя
  числами, карточка текущей работы, её состояния и края. Первая страница
  (`8cdaca5b-...`) не тронута и остаётся в силе.
- **Открытых вопросов по макету нет** — решения заказчика записаны в самой
  странице, раздел «Решено сегодня».
- **Кадра прораба в Figma нет** — проверено по дереву файла
  `bsgJW2RZLQlZbYUq3peqbD`: только руководитель и телефон механика. Меню
  берём из кадров `1:60` и `82:2`.
- **Проверки кода не гонялись**: код не менялся, `make lint` и `make test`
  в этой сессии не запускались.

## Решения этой сессии (в коде их ещё нет)

- **Карточка работы у прораба делается** — прежнее «строка ничего не
  открывает» отменено. Встаёт этапами 9.1 (ТО), 9.2 (заявка), 9.3 (края).
- **Нажимается вся строка** текущей работы; у сданной всё остаётся как было.
- **Нет `contact_phone` — нет кнопки «Позвонить»**: жёлтая пометка «Номер не
  указан» и ссылка «Добавить номер» в карточку сотрудника.
- **Чек-лист в карточке с обрывом** на шестом пункте, пустое задание заявки
  пишется словами «Задание не описано», опечатку «Обьекты» не чиним.

## Не доделано

- **Этап 8.2 (ветка заявок в ручке) не начат** — отложен сознательно: сначала
  фронт, заявки в ручку доедут после. Ручка пока отдаёт только ТО.
- **`total` и `problems` в ответе не заводились** — приезжают вместе с 8.2.

## Следующий этап

**Цель.** Этап 8.3 — раздел «Сейчас в работе» появляется над лентой сданных
работ и живёт своим запросом.

**Готово, когда.** Раздел показывает работы из `/work/in-progress` (пока одни
ТО), пустой раздел не прячется, сбой раздела не роняет ленту сданных.

## Первые шаги

1. **Форму взять из `frontend/lib/screns/submitted_works/`** — там `models/`,
   `repository/`, `bloc/`, `view/`, `widgets/`. Новый раздел кладём соседним
   подкаталогом, а не дописываем в существующий блок.
2. **Место в экране** — `view/submitted_works_screen.dart`, между `_Header` и
   `_FiltersBar`. Отдельный `BlocProvider`: два запроса, два состояния.
3. **Ручка** — `GET /api/v1/work/in-progress`, поля смотреть в
   `backend/src/schemas/in_progress_works.py`.

## Не трогать

- `/work/submitted`, её схемы и `unreviewed-count` — в проде.
- `maintenanceState` и справочник `statuses`.
- `frontend/els_mobile/` — чужая сборка, не наша.
- Строку сданной работы и кнопку «Проверил».

## Уточнить перед стартом

- 8.3 и 8.4 (вёрстка строки) за одну сессию или по одному?
- Незакоммиченная работа 8.1 висит уже вторую сессию — коммитим или ждём?

## Ссылки

- `https://claude.ai/code/artifact/e04f4c07-d01e-45b0-8f36-0eeb04b98e72` —
  утверждённый макет: строка, состояния, края, правила поведения.
- `els-vault/00-home/план - мобильное приложение механика и замыкание процесса.md`
  — этапы 8.1–8.7; карточка (9.1–9.3) в нём ещё не записана.
