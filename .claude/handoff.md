---
этап: S5.2 — оформление внутреннего акта клиенту
статус: закрыт
дата: 2026-09-06
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: S5.2 принят глазами под прорабом, хвост S5.1 закрыт

Кода в этой сессии не написано ни строки — этап лежал в `25f4cb8`, осталась
приёмка. Прошла целиком, правок не потребовала.

## Сделано и проверено

Вживую под `pr@mail.ru` на `localhost:8080`, объект 82 «Создаю Тест»,
дефект 6 «Порван плакат на двери шахты»:

- **«Вид ТО» показывает «ТО 1»** и в строке списка, и в реквизитах карточки
  — последний хвост S5.1 закрыт.
- **Выпуск клиенту**: снял галочку с одного снимка, вписал свой заголовок →
  создан акт 8: `kind=client`, `parent_id=6`, `state=issued`; в
  `defective_act_client_photo` ровно два снимка из трёх. Внутренний акт
  перешёл в `issued`.
- **PDF собран верно**: кириллица целая, «ДЕФЕКТНАЯ ВЕДОМОСТЬ № 8 от
  06.09.2026», в таблице клиентский заголовок, фотографий две — снятый
  кадр в документ не попал.
- **Пилюля «Выдан клиенту»** в шапке и в строке списка; блок «Оформлено
  клиенту» со строкой выпуска, датой и активной «Скачать PDF». Блок
  переживает полную перезагрузку приложения (F5 + повторный заход) —
  связь `client_acts` в `DefectiveActGet` отдаётся как надо.
- **Второй выпуск** кнопкой «Оформить клиенту ещё раз»: заголовок оставлен
  пустым — подставился заголовок механика, снимки все три, в блоке две
  строки. Акт 9, свой PDF.
- **«Скачать PDF»** дёргает `POST /api/v1/files/link` → 200.

`make lint` и `make test` в этой сессии не гонялись: код не менялся.
Последний прогон — прошлая сессия на `25f4cb8`: 975 бэк, 478 фронт.

## Подложено в данные прогона

- Заголовок дефекта 6 в базе поправлен (был задвоен трижды).
- Дефекту 6 подложены 3 снимка: файлы в `static/defective_act/6/photo/`
  контейнера `backend` + строки в `defective_act_photo` — иначе нечего
  было отбирать галочками.

## Не доделано

Ничего по S5.2. Замечено попутно, в этап не бралось:

- В списке объектов объект 82 показан как **«sss»**, хотя называется
  «Создаю Тест» (поиск по «Создаю» его находит, карточка верная).
- «Месяц ТО» и «Год плана» в реквизитах — прочерки: у дефекта от факта ТО
  `planned_to_id` и `month` пустые. Так ли задумано — не выяснено.
- `helper/api_image.dart`: `_blank` описан как прозрачный пиксель, а
  декодируется в полупрозрачный синий.

## Следующий этап

**Цель.** S5.3 — значок-счётчик дефектных актов в окнах «График» и «Объект».

**Готово, когда** в обоих окнах виден красный документик с числом дефектов
за выбранный год, при нуле — серый с подписью «дефектных актов не было»,
а клик открывает список из S5.1.

## Первые шаги

1. Кадр `1182:232` в `~/els-figma/`: у заголовка «Техническое обслуживание»
   уже есть жёлтый документ с «!» — берём его место и вид, число и серое
   состояние дорисовываем сами.
2. Счёт брать из уже живой ленты `GET /api/v1/defective-act/by-object/{id}/?year=`
   — она отдаёт только `internal`, то есть считает ровно то, что нужно.
3. Клик ведёт в `DefectsScreen` (`lib/foreman/defects/defects_screen.dart`),
   точка входа уже есть в `object_page_foreman.dart` (блок «Документы»).

## Не трогать

- Ручку создания вида ТО; `status_id` и `PUT /defective-act/{id}/status/`.
- Легаси `els_mobile/`, `soon_screen.dart`, заглушки `SizedBox.shrink()`.
- Очередь `mechanic/data/outbox.dart` и отложенный ключ.
- `IntTest.indexScreensForeman` и гигантский `build` в
  `object_page_foreman.dart` — только добавлять.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — раздел S5.3.
- `els-vault/knowledge/patterns/тестовые учётки локального стека - какая роль под каким логином.md`
  — вход в браузере начинает человек, пароль агент не вводит.
