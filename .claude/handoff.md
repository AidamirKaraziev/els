---
этап: 7 — набросок «Предпросмотр + окно «Программа модели»»
статус: закрыт
дата: 2026-09-02
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: макет утверждён человеком, в бой он ещё не переехал

**Всё закоммичено**, рабочая копия чиста. Набросок — в `6f24485`:
`frontend/lib/dev/draft/` (три файла), `frontend/lib/dev/schedule_program_draft.dart`
и точка `schedule-program-draft` в `.claude/launch.json`, порт 5603.

## Сделано и проверено

- **Набросок утверждён глазами** на 5603. Логики в нём нет — это макет.
- **Страница «Предпросмотр» в новом виде**: два шага, витрины нет. Над лентой
  карточка «Программа модели — <название>» с кнопкой «Изменить программу»;
  программы нет — красное «не заведена», кнопка «Создать программу», вместо
  ленты объяснение, «Утвердить» погашена.
- **Перенос ТО — цепочкой, не обменом.** Тянешь любую клетку на месяц — весь
  год сдвигается за ней по кругу, порядок видов ТО не меняется; по сути это
  выбор месяца начала цикла руками. Проверено: СТАРТ с марта на май → цикл с
  мая, хвост лёг на январь–апрель.
- **Окно «Программа модели»**: название = марка с моделью из объекта (серая
  плашка, не правится) плюс свободное дополнение; двенадцать позиций с
  выбором вида ТО и перетаскиванием за ручку; «Добавить вид ТО» — заглушка;
  предупреждение «программа принадлежит модели»; «Сохранить» гаснет при
  позиции без вида ТО. Проверено: ТО 12 с 12-й на 5-ю позицию → лента
  пересчиталась; «подъезды 3–4» → «LIFT A388509 — подъезды 3–4».
- **Проверки**: `flutter analyze lib/dev` — чисто. `make lint`, `make test`,
  `flutter test` **не гонялись**: боевой код не менялся ни строкой.

## Не доделано

- **Набросок в бою не живёт**, мастер по-прежнему трёхшаговый; чистка из
  этапа 6 не сделана — перетаскивание в ленте (`month_strip.dart`,
  `object_schedule_card.dart`, `ScheduleObjectCellMoved`, `moveCell`) и
  шаг-витрина на месте.
- В локальной базе освобождён декабрь у `planned_to` 87 (объект 25, 2027).
  Вернуть: `december_to_id = 320`.

## Следующий этап

**Цель.** Решает человек (см. ниже). Мнение агента: сначала чистка, потом
переезд наброска в бой, и только потом «Добавить вид ТО» — иначе новому окну
некуда встать.

**Готово, когда** (для чистки). Мастер из двух шагов, на предпросмотре строка
программы, в ленте ничего не таскается; `flutter test`, `flutter analyze`,
`make lint`, `make test` чисты.

## Первые шаги

1. Убрать перетаскивание из ленты: `onCellMoved` в
   `frontend/lib/screns/schedule/widgets/month_strip.dart` и
   `object_schedule_card.dart`, `ScheduleObjectCellMoved`, `moveCell` в трёх
   файлах `object/repository/`, группа тестов «перенос ТО перетаскиванием» в
   `test/schedule/object_schedule_move_test.dart` (группу «срок ближайшего
   ТО» **оставить**).
2. Удалить `wizard_program_step.dart`, сократить `_titles` в
   `object/wizard/view/schedule_wizard_screen.dart` до двух шагов.
3. Перенести вёрстку из `lib/dev/draft/draft_preview_page.dart` в
   `wizard_preview_step.dart` — дубль клеток и легенды исчезнет.

## Не трогать

- `schedule_page.dart`, `schedule_page_foreman.dart` и прочие экраны
  подрядчика — они в проде.
- Бэкенд целиком: ручки программы (`by-model`, `suggestion`, `PUT`) готовы,
  отдельной программы под один объект не делаем. Права и роли, контракт
  `GET /schedules/rows`, кнопка «Создать шаблон» — тоже мимо.

## Уточнить перед стартом

- **Что делаем следующим: чистку или «Добавить вид ТО».** Человек назвал
  второе, агент возразил (окну некуда встать), ответа не было.

## Ссылки

- `frontend/lib/dev/draft/` — утверждённый набросок, отсюда всё переезжает.
- `frontend/lib/screns/schedule/object/wizard/` — боевой мастер, куда переезжать.
