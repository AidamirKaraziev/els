---
этап: 8 — набросок окна «Новый вид ТО»
статус: закрыт
дата: 2026-09-03
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: набросок дорисован и утверждён, долг этапа 7 не тронут

**Всё закоммичено**, рабочая копия чиста. Этап — в `27e86d5`, правился один
файл: `frontend/lib/dev/draft/draft_program_dialog.dart`.

## Сделано и проверено

- **Окно «Новый вид ТО» вместо заглушки.** Маленькое окно поверх правки
  программы: название, жёлтая подпись про общий справочник, «Отмена» и
  «Добавить». Абзац «здесь будет полное окно» убран.
- **«Добавить» погашена**, пока название пусто. Уже заведённое имя — красная
  рамка и подпись «Такой вид ТО уже есть», кнопка погашена; сверка без учёта
  регистра и крайних пробелов.
- **Справочник видов ТО переехал в состояние окна программы** (`_typeActs`).
  До этого страница пересобирала свой список за спиной открытого окна, и
  новый вид не появлялся в выборе позиции — нашлось при проверке глазами.
- **Проверено на дев-точке 5603**: «Добавить вид ТО» → окно, кнопка погашена
  → «ТО 4» → «Добавить» → «ТО 4» есть в выпадающем списке позиции, окно
  программы при этом открыто; повторное «то 4» даёт красную подпись.
  Утверждено человеком.
- **Проверки**: `flutter analyze lib/dev` — чисто. `make lint`, `make test`,
  `flutter test` **не гонялись**: боевой код не менялся ни строкой.

## Не доделано

- **Enter в поле не проверен.** `onSubmitted` написан, но драйвер браузера не
  доносит нажатия клавиш до Flutter-канваса — доходит только ввод текста.
  Проверять руками.
- **Долг этапа 7 на месте.** Набросок в бою не живёт, мастер трёхшаговый,
  перетаскивание в ленте не убрано, вёрстка клеток задвоена между
  `lib/dev/draft/draft_preview_page.dart` и `wizard_preview_step.dart`.
  Сегодняшняя правка легла только в набросок — копии расходятся дальше.
- **Полное окно вида ТО** (перечень работ чек-листа, копия чужого шаблона) не
  рисовалось: сознательно, подтверждено одно поле.
- В локальной базе освобождён декабрь у `planned_to` 87 (объект 25, 2027).
  Вернуть: `december_to_id = 320`.

## Следующий этап

**Цель.** Чистка и переезд наброска в бой: мастер из двух шагов, на
предпросмотре строка программы, в ленте ничего не таскается.

**Готово, когда.** Мастер двухшаговый, вёрстка предпросмотра одна на бой и
набросок; `flutter test`, `flutter analyze`, `make lint`, `make test` чисты.

## Первые шаги

1. Убрать перетаскивание из ленты: `onCellMoved` в
   `frontend/lib/screns/schedule/widgets/month_strip.dart` и
   `object/widgets/object_schedule_card.dart`, `ScheduleObjectCellMoved`,
   `moveCell` в трёх файлах `object/repository/`, группа тестов «перенос ТО
   перетаскиванием» в `test/schedule/object_schedule_move_test.dart` (группу
   «срок ближайшего ТО» **оставить**).
2. Удалить `object/wizard/widgets/wizard_program_step.dart`, сократить
   `_titles` в `object/wizard/view/schedule_wizard_screen.dart` до двух шагов.
3. Перенести вёрстку из `lib/dev/draft/draft_preview_page.dart` в
   `wizard_preview_step.dart`, окно программы и «Новый вид ТО» — следом.

## Не трогать

- `schedule_page.dart`, `schedule_page_foreman.dart` и прочие экраны
  подрядчика — они в проде.
- Бэкенд целиком: ручки программы (`by-model`, `suggestion`, `PUT`) готовы.
  Ручку на создание вида ТО в этом этапе тоже не заводим.

## Уточнить перед стартом

- **Порядок переезда**: сначала чистка целиком, потом вёрстка, или обе части
  одним заходом. Агент за первое — тесты падают меньшими кусками.

## Ссылки

- `frontend/lib/dev/draft/` — утверждённый набросок, отсюда всё переезжает.
- `frontend/lib/screns/schedule/object/wizard/` — боевой мастер, куда переезжать.
- `.claude/launch.json` — точка `schedule-program-draft`, порт 5603; сервер
  не переживает простоя, поднимать заново.
