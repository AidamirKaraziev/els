---
этап: 10 — раздел «Графики» у прораба и админа
статус: в работе
дата: 2026-08-26
план: els-vault/00-home/план - раздел графики у прораба и админа.md
---

# Передача: фаза 1 закрыта, дальше бэкенд — фаза 2

Всё закоммичено: `f40eab8 feat(графики): новый раздел встаёт на место
старого у обеих ролей`, рабочая копия чистая. Пункт 1.7 закрыт, кроме
удаления старых экранов — отложено сознательно.

## Сделано и проверено

- **Оба входа ведут в новый раздел**: `home_page.dart:70` —
  `ScheduleSection(role: admin)`, `home_foreman.dart:56` — то же с
  `role: foreman` и `drawer: DrawerForeman()`.
- **Меню раздел получает снаружи** — параметр `drawer` в
  `view/schedule_section.dart` идёт в оба экрана и в ленту участка;
  паттерн взят у `ReportScreen({this.drawer})`.
- **Кнопка слева в шапке** — общий `scheduleShowsLeading(context)` там же:
  «назад» на вложенном экране всегда, гамбургер только при ширине ≤ 1350
  (`kScheduleShellWideWidth`); шире меню держит оболочка колонкой.
- **Мёртвая предзагрузка убрана**: `my_drawer.dart`, `drawer_foreman.dart`
  и `initState` оболочки прораба не тянут список объектов перед входом —
  он кормил только экран подрядчика.
- **Проверки**: `flutter test` целиком — **304 теста, все проходят** (было
  296; +3 в `month_strip_test.dart`, +5 в новом
  `schedule_section_drawer_test.dart`). `flutter analyze` по разделу и
  `test/schedule` — 0 замечаний. `flutter build web --release -t
  lib/main.dart` собирается; глазами на 1440 и 375 через
  `main_schedules.dart`. Flutter: `~/development/flutter/bin`.

## Не доделано

- **Старые `schedule_screen.dart` и `schedule_screen_foreman.dart` не
  удалены** — решение человека: за ними единственный вход в редактор
  графика ТО (`schedule_page.dart`, `schedule_page_foreman.dart`,
  `changeTOGraphics`), а новая карточка раздела — просмотр, не замена.
  Из UI недостижимы, но держат глобалы подрядчика, которые читают
  выжившие файлы.
- **Боевой вход руками не пройден**: нужен логин к бэкенду, стек локально
  не поднимался. Клик в браузерном пейне до канваса не доходит — открытие
  меню закрыто тестами, а не глазами.
- **Кадр `3:339` не сверен**: Figma REST отдавала 429 до 28 августа; обход
  — `File → Export frames to PDF` из самой Figma.
- `make lint` и `make test` не гонялись: обе про бэкенд. Нужны в фазе 2.

## Следующий этап

**Цель.** Фаза 2 — раздел живёт на боевых данных, а не на фикстуре.

**Готово, когда.** `GET /schedules/rows` отдаёт строку «объект + 12 клеток»
с посчитанным на сервере состоянием, фикстура в проде не используется,
`make lint` и `make test` зелёные.


## Первые шаги

1. Чек-лист фазы 2 — план, строка 175 и дальше (пункт 2.1).
2. Разворот «12 колонок → 12 строк» уже написан:
   `CrudActFact._planned_cells()` (`backend/src/crud/crud_act_fact.py:77`) и
   `_MONTH_PLANNED_COLUMN` (`backend/src/crud/crud_planned_to.py:29`) —
   переиспользовать, второй копии не делать.
3. Состояние клетки — `_maintenance_query` в
   `backend/src/crud/crud_reports.py`.

## Не трогать

- `schedule_page.dart` и `schedule_page_foreman.dart` — редактор графика ТО.
- Контракт живых ручек: всё новое — необязательные query-параметры либо
  новые пути; без параметров поведение прежнее байт в байт.

## Уточнить перед стартом

- **Судьба редактора графика ТО**: переносить в новый раздел, оставлять
  как есть или выкидывать. Токен Figma из переписки 25 августа не отозван.

## Ссылки

- `els-vault/00-home/план - раздел графики у прораба и админа.md:175` — фаза 2.
- `frontend/test/schedule/` — девять файлов, образец для новых тестов.
