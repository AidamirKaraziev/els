---
этап: S0.1 — путь кадра из Figma
статус: в работе
дата: 2026-09-01
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: путь кадра налажен, самого кадра ещё нет

Работа закоммичена: HEAD `6473861`. Рабочая копия чиста.

## Сделано и проверено

- **Dev Mode MCP отпал окончательно.** У аккаунта план Starter, там его нет
  вовсе («No access to the Dev Mode MCP server»). Платить не будем — решение
  от 27 августа переписано целиком.
- **Заведена `~/els-figma/`** (вне репозитория) с `README.md`: как выгрузить
  кадр PNG 2x, как назвать файл, ключи кадров и ключ файла «КПЭК»
  `bsgJW2RZLQlZbYUq3peqbD`. Пока в папке только README.
- **`scripts/figma_frame.py`** качает PNG и структуру узла (числа: отступы,
  цвета, шрифты) в `~/els-figma/`. Токен читает из `~/.config/figma-token`,
  в репозиторий и в вывод не попадает.
- **REST на Starter проверен вживую и оказался ночным инструментом.**
  `/v1/me` — 200, а первый же запрос к `files/:key/nodes` вернул `429` с
  `Retry-After: 59200` (16,5 ч). В скрипт добавлено ожидание по этому
  заголовку. Основной путь — ручной экспорт.
- **Vault приведён в согласие**: старая заметка про локальный MCP удалена,
  вместо неё `кадры макета экспортируются вручную в PNG`, 12 ссылок `[[…]]`
  переписаны, правило про фронт в `CLAUDE.md` обновлено, `S0.1` в плане
  переписан под ручной экспорт.
- **Проверки**: `make lint` и `make vault-check` чисты. Тесты не гонялись —
  код проекта не тронут. Сам скрипт под линт не попадает: он берёт только
  `backend/src`.

## Не доделано

- **Кадр `1182:232` так и не прочитан** — единственный незакрытый пункт
  «Готово, когда» у `S0.1`, и он за человеком: выгрузить PNG 2x в
  `~/els-figma/`.
- **Токен Figma не сменён.** Тот, что лежит в `~/.config/figma-token`, — это
  засветившийся в переписке. Отозвать его и завести новый read-only.
- **`S0.2` не начат** — ревизия кадров под все задачи `S2.*`, `S4.*`, `S5.*`.

## Следующий этап

**Цель.** Закрыть `S0.1` живым кадром, пройти `S0.2` и войти в `S1.1` —
модель программы обслуживания у модели оборудования и миграция.

**Готово, когда.** Кадр «Окно отдельного графика» прочитан, а у каждой
задачи по фронту проставлен ключ кадра либо «кадра нет, рисуем набросок».

## Первые шаги

1. Проверить, лежит ли в `~/els-figma/` PNG кроме README. Нет — попросить
   выгрузить, сам достать не могу.
2. Если прошло больше суток с 28 августа, квота могла отпустить:
   `python3 scripts/figma_frame.py 1182:232 окно-график-объекта`.
   Снова `429` — не ждать, идти ручным путём.
3. Отметить закрытые пункты в `S0.1` и браться за `S0.2`.

## Не трогать

- Контракт живых ручек: новое — только новые пути либо необязательные
  параметры.
- Правила доступа в `core/access.py`: «прораб видит всё» — решение
  заказчика.
- Сторонние Figma-MCP (Framelink и подобные): под капотом тот же REST с тем
  же токеном и квотой.

## Уточнить перед стартом

- Сменён ли токен Figma — от этого зависит, можно ли считать `S0.1` закрытым
  целиком.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — `S0.1` и `S0.2` с
  отметками, дальше весь план на шесть этапов.
- `~/els-figma/README.md` — инструкция по выгрузке и ключи кадров.
- `els-vault/knowledge/integrations/кадры макета экспортируются вручную в PNG.md`
