---
этап: 13 — точечное обновление строки в ленте «Графиков»
статус: закрыт
дата: 2026-09-04
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: долг ленты закрыт, дальше этап 3 плана — дефектные акты

Работа закоммичена: `cd59b0b`, рабочая копия чистая.

## Сделано и проверено

- **Закрыли ТО — клетка месяца в ленте раздела меняет цвет сразу по
  возврату**, отбор и догруженные страницы на месте. Событие
  `SchedulesRowRefreshed(objectId)`; `_requestId` оно не поднимает — иначе
  отменило бы едущую догрузку страницы.
- **Строку берём у сервера** ручкой `SchedulesRepository.fetchRow` — тот же
  `GET /schedules/rows?object_id=&year=`, что у экрана объекта.
- **Оба пути закрытия дёргают обновление**: клетка прямо из ленты и экран
  графика объекта. `ScheduleObjectOpener.open` получил необязательный
  `onScheduleChanged`; он же срабатывает после расстановки графика мастером.
- **Проверки**: `flutter test` — 409 зелёных (было 404 + пять новых в
  `test/schedule/schedules_row_refresh_test.dart`); `flutter analyze` — вне
  легаси `els_mobile/` ошибок нет. Бэкенд не трогали, `make lint` и
  `make test` не гонялись.
- **Приёмка глазами**: `make up`, localhost:8080, 1280 px, «1аСпортмастер»
  за 2027 с прокруткой до «Это все объекты». Закрыт август клеткой из ленты
  и сентябрь через экран объекта — оба раза клетка стала «выполнено», список
  остался догруженным, `page=1` после закрытия не запрашивался.

## Не доделано

- **Клики CDP до Flutter-канвы не доходят** (`computer{left_click}` в
  браузерной панели молчит). Обход: включить дерево доступности —
  `document.querySelector('flt-semantics-placeholder').click()`, дальше
  жать `.click()` на нужном `flt-semantics` по `aria-label`. Прокрутка
  работает событиями `WheelEvent` на `flutter-view`.
- **Ветка «Позже» окна «Расставить график ТО» глазами не пройдена** (долг
  этапа 12): залипают выпадающие списки формы подрядчика. Держится на тесте.
- **Под админом пути не проверены**: пароли агент не вводит, сессия
  прорабская. Классы те же, отличается роль.
- **Ручка создания вида ТО не заведена** (с этапа 10): `types_acts.id` без
  автоинкремента и равен периодичности, есть только `GET /type-acts/`; схемы
  и `crud_type_acts` готовы. Открыт вопрос — откуда брать `id`.

## Следующий этап

**Цель.** Этап 3 плана — бэкенд дефектных актов, S3.1: миграция модели под
четыре точки входа.

**Готово, когда.** Модель принимает все четыре точки входа, миграция
накатывается и откатывается, `make test` зелёный.

## Первые шаги

1. Прочитать этап 3 в `els-vault/00-home/план - окно график объекта.md` —
   там перечислены четыре точки входа.
2. Смотреть модели `backend/src/models/` рядом с `acts_fact`; миграции
   через `make migration`, проверка `make lint` и `make test`.
3. Второй классификации статусов не заводить: считает `MaintenanceStatus`
   в `backend/src/schemas/reports.py`.

## Не трогать

- Заглушки `SizedBox.shrink()` в `_screens` и `_screensForeman`.
- Легаси `els_mobile/`; ручку создания вида ТО — пока не решён вопрос `id`.

## Уточнить перед стартом

- Данные стенда чистим или оставляем: у объекта 25 за 2027 закрыты шесть
  актов прошлой сессии (`acts_fact.id` 309–314) и два этой — август и
  сентябрь; плюс объект «Проверка S2.5 предложение графика».
- Идём в этап 3 целиком или только S3.1 за сессию.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — этап 3 и дальше.
- `frontend/lib/screns/schedule/bloc/schedules_bloc.dart` — все события
  ленты раздела в одном месте.
