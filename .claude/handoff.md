---
этап: S4.1 — механик заводит дефект из работы по ТО
статус: в работе
дата: 2026-09-04
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: лист «Дефект» нарисован, отправки ещё нет

Набросок закоммичен: `fc3b506`. Рабочая копия чистая. Остановились ровно на
границе «макет утверждён — логика не написана»: в обработчике заглушка.

## Сделано и проверено

- **Лист «Дефект» есть** — `frontend/lib/mechanic/screens/defect_sheet.dart`:
  заголовок обязателен (без него кнопка серая), подробности и до пяти
  снимков по желанию, снимок убирается до отправки. Вид посмотрен в браузере
  на превью.
- **Кнопка «Дефект»** встала третьим тихим действием карточки ТО
  (`act_screen.dart`), новое поле `ActControls.canReportDefect`
  (`data/tasks.dart`): доступно из начатой и незакрытой работы, включая
  вставшую.
- **Превью** `lib/dev/defect_sheet_preview.dart` + запись
  `defect-sheet-preview` в `.claude/launch.json`, порт 5604. Лист
  открывается сам — клики из отладчика до Flutter-канвы не доходят.
- **Проверки**: `flutter analyze lib/mechanic` — только прежние
  `withOpacity`; `flutter test test/mechanic/` — **117 зелёных**, из них 6
  новых на лист. Бэкенд не трогали, `make lint` и `make test` не гонялись.

## Не доделано

- **Отправки нет.** `_reportDefect` в `act_screen.dart` показывает снекбар
  вместо запроса. Это весь остаток этапа.
- **Долг по картинкам закрыт чтением кода, а не глазами.** Разведка
  показала: фронт уже ходит за файлами через `apiImage` с токеном (110
  мест), выгрузки — через `POST /files/link`, живых прямых обращений к
  `/api/v1/static/` нет. На `make up` не проверяли.
- **`NetworkImage('assets/user.png')`** в шести файлах (`object_page.dart`
  и соседние) — должен быть `AssetImage`. Баг подрядчика, к этапу не
  относится.
- Долги прошлых этапов живы: бэкфилл `object_id` на боевых данных
  (`d8a6c3f95b21`) глазами не смотрели; ветка «Позже» окна «Расставить
  график ТО» и пути под админом не пройдены; ручка создания вида ТО не
  заведена (открыт вопрос `id`).

## Следующий этап

**Цель.** Дописать S4.1: лист отправляет дефект, в том числе без связи.

**Готово, когда.** Дефект из открытой работы уходит в `POST /defective-act/`
с `act_fact_id`, снимки — в `POST /defective-act-photo/{id}/`, прораб видит
акт, закрытию работы форма не мешает. Оффлайн: акт и фото уходят по порядку
после появления связи, фото привязано к своему акту.

## Первые шаги

1. `MechanicWorkspace.sendDefect(...)` в `data/mechanic_workspace.dart` —
   рядом с `sendActChecklist` и `attachStepPhoto`; тело
   `{act_fact_id, title, description}`, обязателен только `title`.
2. **Отложенный `id` в очереди** (решено в этой сессии): `OutboxAction`
   получает `provides` (сохраняется) и `createdId` (заполняет отправщик из
   ответа), путь фото ставится как `/defective-act-photo/{ключ}/`, `_flush`
   подставляет известное. Неразрешённый ключ без давшего его действия —
   в «отклонённые». Карта чистится в `forget()`.
3. Тесты в `frontend/test/mechanic/outbox_test.dart`: подстановка, порядок,
   отклонение осиротевшего снимка, очистка при `forget()`.

## Не трогать

- Ручку создания вида ТО — вопрос `types_acts.id` не решён.
- `status_id` и `PUT /defective-act/{id}/status/` — устарели, но живы.
- Вход в форму с пункта чек-листа (`checklist_step_id`) — отдельный шаг.
- Легаси `els_mobile/`; заглушки `SizedBox.shrink()` в `_screens` и
  `_screensForeman`.

## Ссылки

- `els-vault/00-home/журнал - изменения контракта API для фронта.md` —
  раздел «Ручки дефектных актов (2026-09-04)».
- `frontend/lib/mechanic/data/outbox.dart` — правила очереди, которые
  нельзя нарушить отложенным `id`.
- `backend/src/schemas/defective_act.py` — что принимает `POST /defective-act/`.
