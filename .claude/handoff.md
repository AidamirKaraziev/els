---
этап: 9 — чистка и переезд наброска программы в бой
статус: закрыт
дата: 2026-09-03
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: новый экран открывается из боевых «Графиков», набросок снят

Ничего не закоммичено: вся работа в рабочей копии поверх `444a9ac`.

## Сделано и проверено

- **Набросок снят.** Удалены `frontend/lib/dev/draft/` (три файла) и
  `frontend/lib/dev/schedule_program_draft.dart`, из `.claude/launch.json`
  убрана запись `schedule-program-draft` (порт 5603). Ссылок не осталось.
- **Экран переехал в бой маршрутом.** Новый
  `lib/screns/schedule/view/route_schedule_object_opener.dart` кладёт
  `ScheduleObjectScreen` через `Navigator.push` поверх оболочки; прежний
  `shell_schedule_object_opener.dart` удалён вместе с номерами экранов 12/14 и
  загрузками в глобальные переменные подрядчика. `ScheduleObjectOpener.open`
  теперь принимает `BuildContext`. Год доезжает: `initialYear: row.year`.
  Маршрут не ждём (`unawaited`) — future от `push` завершается при закрытии
  экрана, и ожидание держало бы ленту под индикатором всё время просмотра.
- **Проверено глазами на собранном стеке** (`make up`, localhost:8080), под
  прорабом: «График» → год 2027 → строка «1аСпортмастер ↑» открывает новый
  экран с лентой ТО на 2027; «назад» возвращает в ленту с тем же годом и
  отбором.
- **Проверки**: `flutter analyze` по затронутому — от наших правок ни ошибок,
  ни предупреждений; `flutter test` — 391 тест зелёный. Бэкенд не трогали,
  `make lint` и `make test` не гонялись.
- **План поправлен**: в `план - окно график объекта.md` проставлены галочки
  S2.3 и S2.4 (сделаны в прошлые сессии, отметить забыли) и первый пункт S2.6.

## Не доделано

- **Под админом путь не проверен руками** — сессия в браузере была
  прорабской, пароли агент не вводит. Класс тот же, отличается только роль.
- **Экраны подрядчика на месте**: `schedule_page.dart` (5200 строк),
  `schedule_page_foreman.dart` (4713), `schedule_screen*.dart` и мёртвая
  `getAllTOGraphics`. Из «Графиков» в них больше не заходят — снимать их это
  остаток S2.6.
- **Ручка создания вида ТО не заведена.** Разведка: `types_acts` — `id` без
  автоинкремента, равен периодичности; есть только `GET /type-acts/`; схемы
  `TypeActCreate`/`TypeActUpdate` и `crud_type_acts` готовы, миграция не
  нужна. Открыт вопрос — откуда брать `id`.
- В базе у `planned_to` 87 (объект 25, 2027) освобождён декабрь: вернуть
  `december_to_id = 320`. Решили оставить как есть.

## Следующий этап

**Цель.** Доснять S2.6: убрать экраны подрядчика и расставить значки «?».

**Готово, когда.** В проекте нет `schedule_page*.dart` и `schedule_screen*.dart`,
`flutter analyze` чист, `flutter test` зелёный, и пройдена приёмка S2.6 глазами.

## Первые шаги

1. Грепом снять всех, кто ссылается на `schedule_page.dart`,
   `schedule_page_foreman.dart`, `schedule_screen*.dart` и `getAllTOGraphics` —
   там же лежат номера экранов 12 и 14 в списках `home_page.dart` и
   `home_foreman.dart`.
2. Удалять по одному файлу, между удалениями — `flutter analyze`: файлы
   подрядчика объявляют одноимённые глобальные переменные.
3. Значки «?» — из `frontend/lib/helper/hints/hints.dart`.

## Не трогать

- Бэкенд: ручку создания вида ТО не заводим, пока не решён вопрос про `id`.
- Легаси `els_mobile/` — его ошибки анализа не наши.
- Неиспользуемый импорт `my_test_screen.dart` в `home_page.dart:18` — он
  оттуда и был, уйдёт вместе с чисткой оболочки.

## Уточнить перед стартом

- Приёмка S2.6 — пять случаев (объект без графика; половина ТО выполнена;
  модель без шаблона на ТО6; переключение года; окно 1280 px). Проходим их в
  этой же сессии или отдельной?
- Ручка создания вида ТО: `id` задаёт человек, считаем `max(id)+1` или чиним
  последовательность?

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — S2.6 и дальше S2.5.
- `frontend/lib/screns/schedule/view/route_schedule_object_opener.dart` —
  единственный вход в экран графика из боевой оболочки.
