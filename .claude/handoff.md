---
этап: 10 — раздел «Графики» у прораба и админа
статус: в работе
дата: 2026-08-26
план: els-vault/00-home/план - раздел графики у прораба и админа.md
---

# Передача: лента отрисована и показуема, дальше — фильтры и поиск

Ничего не закоммичено. Старые экраны, `main.dart` и бэкенд не тронуты.

## Сделано и проверено

- **Экран запускается.** `frontend/lib/main_schedules.dart` — отладочный вход:
  `BlocProvider<SchedulesBloc>` над `SchedulesScreen`, локаль `ru`, фикстура.
  `flutter run -d web-server` в браузере не работает (вебсокет debug не
  апгрейдится): смотреть через `flutter build web --release -t
  lib/main_schedules.dart` (~160 с) + `python3 -m http.server` из `build/web`.
- **Кадр `83:312` разобран**: `/v1/images` до сих пор `429`, а
  `/v1/files/bsgJW2RZLQlZbYUq3peqbD/nodes?ids=83:312` — `200`. **Строка
  переписана по нему**: ряд высотой 80 — название · заводской номер · серая
  плашка «адрес + прораб» с иконками · зелёная пилюля типа · лента. Была
  колонка, пилюля типа была серой.
- **`MonthStrip`**: скролл убран, клетка сжимается, подпись по влезанию
  (`ТО 1` → номер месяца → без подписи), тултип «Январь · ТО 1 · выполнено не
  вовремя». Месяцы из `kMonthsNominative` (`helper/calendar/month_picker.dart`),
  не через `DateFormat`.
- **Порог складывания в колонку — 970**, посчитан от ленты
  (`213 · 91/22 + 48 + 36 ≈ 965`). Прошлые 700 обрезали ленту: на 800 px было
  видно 10 клеток из 12, молча. В карточку теперь уходит `row.nameLabel` —
  строка отдаёт наверх пару «строка и клетка».
- **Проверки**: `flutter analyze` по новым файлам — 0 замечаний, `flutter test`
  целиком — **271 тест, все проходят**. Глазами на 1440 / 1010 / 960 / 375:
  двенадцать клеток, пять состояний, переливов нет.

## Не доделано

- **Клик по клетке не проверен руками.** В браузерной автоматизации `onTap` не
  срабатывает вовсе — ни на клетке, ни на стрелке года (год остался 2026),
  хотя события до CanvasKit доходят и тултип всплывает. Закрыто тестом
  `test/schedule/schedules_screen_open_card_test.dart` — маршрут перехватывается
  наблюдателем. **Живьём перехода никто не видел.**
- Карточка за клеткой тянет данные по сети (`WorkDetailsBloc`), локально бэка
  нет — что она покажет с настоящей ручкой, неизвестно.
- По договорённости вне сессии: фильтры, поиск, участки, подгрузка по скроллу,
  подмена старых входов.
- `make lint` и `make test` не гонялись: первая — ruff по `backend/src`, вторая
  поднимает контейнер с тестовой БД и гонит pytest. К фронту обе не относятся.

## Следующий этап

**Цель.** Панель фильтров и поиск над лентой — пункты 1.3 и 1.4 плана.

**Готово, когда.** Над списком стоят пять выпадающих фильтров (Название,
Заводской номер, Участок, Тип, Графики) и поиск по лупе; они складываются и
сужают выдачу фикстуры; активный фильтр показывает значение и крестик сброса.

## Первые шаги

1. Открыть экран и **самому нажать на цветную клетку** — единственное, что
   осталось непроверенным руками.
2. Панель фильтров — карточка над списком, кадр `83:312`, группы `Group 117` и
   `Group 196..201`: пилюля 24 px, текст `fs=10`, треугольник-стрелка справа.
   Значения — из `SchedulesRepository.fetchFilterOptions()`, фикстура их отдаёт.
3. Поиск: `TextEditingController` + debounce 300 мс, класть в
   `ScheduleFilters.search` и слать тем же `SchedulesRequested`. Складывать с
   фильтрами модель уже умеет — см. `toQuery()`.

## Не трогать

- `schedule_screen.dart`, `schedule_screen_foreman.dart`, `schedule_page.dart`,
  `schedule_page_foreman.dart` — живут до конца фазы 1. Их ~80 замечаний в
  `flutter analyze` — обычный фон, не регресс.
- Бэкенд целиком: новые ручки, `ilike` и индексы — фаза 2.
- `main.dart`, `home_page.dart`, `home_foreman.dart` — входы в конце фазы.

## Уточнить перед стартом

- Токен Figma засветился в переписке 25 августа. Отозвать и перевыпустить:
  Figma → Settings → Security. Висит с прошлой сессии.

## Ссылки

- `els-vault/00-home/план - раздел графики у прораба и админа.md` — чек-лист
  фазы 1, пункты 1.3 и 1.4.
- `frontend/test/schedule/` — 10 тестов, образец для новых.
