---
этап: 0 — подготовка (S0.1, S0.2)
статус: закрыт
дата: 2026-09-01
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: кадры размечены, этап 0 закрыт, дальше бэкенд

Работа закоммичена: HEAD `42cbd3a`. Рабочая копия чиста.

## Сделано и проверено

- **`S0.1` закрыт.** Кадр `1182:232` «Окно отдельного графика» выгружен и
  прочитан (`scripts/figma_frame.py`, PNG 2x + JSON узла в `~/els-figma/`).
  Старый засветившийся токен Figma отозван, взамен read-only в
  `~/.config/figma-token`, `/v1/me` отвечает 200.
- **`S0.2` закрыт.** В плане у каждой задачи фронта стоит либо ключ кадра,
  либо пометка «**Кадра нет** — набросок». Кадр есть только у `S2.1`, `S2.2`
  (частично), `S2.3` и `S5.3`; у `S2.4`–`S2.6`, всех `S4.*`, `S5.1`, `S5.2`,
  `S5.4` кадров нет вовсе.
- **Разбор кадра и расхождения записаны в `S0.2`**: переключателя года в
  макете нет, подписей «ТО 1/3/6/12» на клетках нет, работы раскрываются
  аккордеоном вместо перехода в `ScheduleWorkCardScreen`, а жёлтый документ
  с «!» у заголовка «Техническое обслуживание» годится под счётчик
  дефектных актов из `S5.3`.
- **Кадр `3:339` — список участков, а не лента объектов.** Лента — `83:312`.
- **Квота Figma измерена.** REST дал два кадра подряд и встал: `429` с
  `Retry-After: 389515` (≈4,5 суток, отпустит ~6 сентября). Лимит на
  аккаунте: новый токен получает тот же `429` с тем же счётчиком. Скрипт в
  таком случае надо убивать, а не ждать заголовок.
- **Проверки**: `make vault-check` чист. `make lint` и `make test` не
  гонялись — код проекта не тронут, менялись только заметки.

## Не доделано

- **Три кадра ждут ручной выгрузки** в `~/els-figma/`, PNG 2x:
  `лента-графиков_83-312.png`, `личный-кабинет-админ_275-764.png`,
  `личный-кабинет-механик_1826-283.png`. До ~6 сентября REST их не отдаст.
  На бэкенд-этап 1 это не влияет.
- **Полный перечень кадров страницы «Руководитель | Администратор» не снят**
  — упёрлось в ту же квоту. Размечали по таблице ключей в
  `~/els-figma/README.md`.

## Следующий этап

**Цель.** `S1.1` — программа обслуживания как сущность у модели
оборудования: две таблицы, миграция, права.

**Готово, когда.** Миграция накатывается и откатывается на локальной базе,
`make test` зелёный.

## Первые шаги

1. Прочитать `S1.1` в плане — там весь состав полей и решение, почему
   позиции лежат отдельной таблицей, а не строкой из двенадцати имён.
2. Модели рядом с `backend/src/models/planned_to.py`; вид ТО — внешний ключ
   на `types_acts` (`core/db/init_db.py:382`, id равен периодичности).
3. Миграция Alembic поверх
   `2026_08_26_1200-b6e4a9d31c05_trigram_indexes_for_object_search.py`.
   Бэкфилла нет — программы заводятся руками.
4. Права `MAINTENANCE_PROGRAM_READ` / `_WRITE` в `core/permissions.py`:
   чтение — у кого есть `PLANNED_TO_READ`, запись — админ и прораб.
5. `make lint` и `make test`.

## Не трогать

- `planned_to` — 12 колонок месяцев остаются как есть, миграции таблицы
  плана в этой работе нет (решение заказчика 3).
- Контракт живых ручек: новое — только новые пути либо необязательные
  параметры.
- `core/access.py`: «прораб читает всё» — решение заказчика.
- Фронт. Этап 1 целиком бэкендовый, экран графика начинается с `S2.1`.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — `S1.1` и дальше весь
  план; в `S0.2` разбор кадра и расхождения с макетом.
- `~/els-figma/README.md` — ключи кадров, как выгружать руками, что уже лежит.
