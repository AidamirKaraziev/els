---
этап: E02·S05 — раздел дефектных актов в PDF отчёта
статус: закрыт
дата: 2026-09-11
план: .claude/plan/E02-defektnye-akty-vezde.md
---

# Передача: S05 закрыт и утверждён глазами; дальше — замечания пользователя к отчёту, первым приоритетом

## Сделано и проверено

- `backend/src/services/reports_pdf.py`: `_defects_section` — раздел
  «Дефектные акты за период» на своей странице после подписи, до подробностей
  по объектам: таблица № · Дата · Объект · Адрес · Акт · Статус · Ответственный
  · Фото, `repeatRows=1`. Фото в разделе не печатаются (только внутри объекта).
- Сводка: «Дефектных ведомостей» → «Дефектных актов»; подзаголовок в объекте —
  «Дефектные акты».
- Тесты `TestPdfDefectsSection` (2). `make lint` чист, `make test` — 1003.
- Стенд `make up` под прорабом, 2026 год: раздел — стр. 3, 6 строк = плитка;
  кириллица цела; `with_photos=true` собирается, фото только в объектах.
  Пользователь утвердил вид.
- Не закоммичено: `reports_pdf.py`, `tests/test_api_reports.py`, `.claude/*`.

## Не доделано

- S06 плана (проверка на стеке, удаление замещённого кода) отложен: пользователь
  поставил вперёд свои замечания к отчёту (ниже).

## Следующий этап

**Цель.** Замечания пользователя к экрану «Отчёты» и PDF — план, макет,
опрос, потом код. Записать в план E02 как S07 (до S06):

1. **Выбор лифтов чекбоксами** в матрице отчёта и PDF/Excel по выбранным.
   Бэк: параметр `object_ids` (список) во всех `/reports/works*` в
   дополнение к `object_id` (старый оставить). Фронт: чекбоксы в строках
   матрицы, кнопка «PDF по выбранным».
2. **Галочка «с фотографиями»** — в UI её нет: кнопка PDF в
   `frontend/lib/screns/report/report_screen.dart:388` шлёт
   `WorksReportExportRequested(format: 'pdf')` без `withPhotos`, хотя блок и
   репозиторий поле пробрасывают (`works_report_event.dart:36`).
3. **«Не все лифты / нет объектов с актами»** — разобрано: в PDF объекты с
   актами есть (стр. 14–16, после объектов с ТО), в файл идёт весь отбор
   (`reports.py:234`, `limit=None`); экран листает по 25 (`_Pager`,
   `report_screen.dart:860`). Решить с пользователем: нужен ли на экране
   режим «все объекты» / больший размер страницы; сверить конкретный лифт,
   если он его назовёт.

**Готово, когда.** Макет чекбоксов и галочки фото утверждён; PDF по двум
выбранным лифтам содержит только их; `with_photos` включается с экрана.

## Первые шаги

1. `/plan E02` — добавить S07 с тремя пунктами выше, S06 сдвинуть после.
2. Опросить пользователя: где чекбоксы (матрица / отдельный список),
   «выбрать все» на странице или по отбору, где галочка фото, нужен ли
   «все объекты» на экране. Есть кадр в `~/els-figma/` — по нему.
3. Набросок на фикстуре (`fixture_works_report_repository.dart`) → показать →
   потом `object_ids` в `crud_reports._defects_query`/`_orders_query`/матрице.

## Не трогать

- Раздел актов в PDF и `_defects_section` — утверждён.
- Ручки `/reports/works*` — только добавлять `object_ids`, не менять ответы.
- `LiveDefectsBadge`, `DefectsScreen`, `crud_schedules._defects_count`.

## Уточнить перед стартом

- Чекбоксы: в матрице экрана или отдельный список объектов?
- «Выбрать все» — по текущей странице или по всему отбору?
- Нужен ли на экране режим «все объекты» вместо страниц по 25?

## Ссылки

- `.claude/plan/E02-defektnye-akty-vezde.md` — сюда добавить S07.
- `backend/src/api/api_v1/endpoints/reports.py` — фильтры экспорта (~204–240).
- `frontend/lib/screns/report/report_screen.dart` — кнопки выгрузки (~381), пейджер (~860).
