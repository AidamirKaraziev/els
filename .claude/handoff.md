---
этап: 3 — лента 12 месяцев на экране объекта (S2.2)
статус: закрыт
дата: 2026-09-02
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: лента живёт и ходит в сеть, дальше переходы

**Ничего не закоммичено.** Вся работа сессии лежит в рабочей копии поверх
`056b7c1`; текст коммита выдавался через `/commit-message`.

## Сделано и проверено

- **Блок «Техническое обслуживание»** — `frontend/lib/screns/schedule/object/
  widgets/object_schedule_card.dart`. Двенадцать клеток цветом, «‹ 2026 ›» в
  шапке блока, пустой год — контуры плюс кнопка «Создать график на 2027»
  (админ и прораб). Смотреть: `flutter run -t lib/dev/
  schedule_object_preview.dart -d chrome`.
- **Переиспользовано, а не написано заново**: `MonthCell`, `MonthStrip`,
  `ScheduleYearPicker` из раздела «Графики». У `MonthStrip` появился
  необязательный `showMonthLabels` (по умолчанию `false` — лента объектов не
  изменилась).
- **Расхождения с кадром `1182:232`** записаны комментарием в виджете:
  подписей на клетках и переключателя года в кадре нет; цвета взяты
  насыщенные из палитры, а не бледные плашки кадра. Иконки выгрузки и
  дефектных актов — S3, строка «11 января – 17 января» — S2.3.
- **Блок**: события `ScheduleObjectYearRequested` и
  `ScheduleObjectGenerateRequested`; карточка объекта при переключении года
  не перезапрашивается, ответ за уже неактуальный год выбрасывается. Ошибка
  ленты (в том числе 422 «нет шаблона») — текстом под лентой, не всплывашкой.
- **Сеть**: `api_schedule_object_repository.dart` — лента из
  `GET /schedules/rows?object_id=&year=`, карточка из `GET /object/{id}/`
  плюс отдельный `GET /organization/{id}/` за названием (в `ObjectGet` она
  голым `id`; не ответил — прочерк), кнопка — `POST /planned-to/generate/`.
- **Бэкенд, одна добавка**: необязательный `object_id` у `/schedules/rows`.
  Условие в `_object_conditions` уже было, состояния клеток по-прежнему
  считает сервер. Запись в журнал контракта добавлена.
- **Проверки этой сессии**: `flutter test` — 339 passed, `flutter analyze` по
  новым файлам чист, `make lint` чист, `make test` — 916 passed. Глазами в
  браузере на 1440 и 430 px: заполненный год, пустой год, нажатие кнопки.

## Не доделано

- **Экран в ленту не вшит и сетевой репозиторий нигде не подставлен** —
  сознательно: точка по умолчанию всё ещё `SchedulePage` подрядчика.
- **Клик по клетке молчит** — заглушка `onCellTap` в
  `view/schedule_object_screen.dart`, переход это S2.3.
- **Шапки оболочки нет** — на фикстуре её не поднять, ждёт встраивания.
- **Кадры `275:764` и `1826:283`** (карточка сотрудника) не выгружены.
  Квота Figma REST отпустит ~6 сентября, сам достать не могу.

## Следующий этап

**Цель.** `S2.3` — переходы из графика: клетка ведёт в карточку работы,
прораб и механик — в карточку сотрудника.

**Готово, когда.** Из графика открывается чек-лист ТО и профиль сотрудника,
и «назад» возвращает в тот же год графика.

## Первые шаги

1. Прочитать `S2.3` в плане: там записано расхождение — в кадре работа
   раскрывается аккордеоном на месте, а не отдельным экраном.
2. Клик по клетке — заменить заглушку `onCellTap` в
   `view/schedule_object_screen.dart` на `ScheduleWorkCardScreen`
   (`screns/schedule/view/`, фаза `standalone`), `cell.actId` уже есть.
3. Клик по ответственным — `screns/employee/view/employee_page.dart`. У
   `ScheduleResponsible` (`object/models/`) сейчас нет `id` сотрудника: его
   надо добавить и достать в `api_schedule_object_repository.dart` из
   `foreman_id.id` / `mechanic_id.id`.
4. Проверить право `USER_READ` у прораба: в роль входит, но экран
   сотрудника открывался только из «Сотрудников».

## Не трогать

- Бэкенд графика: `preview`, `generate` и добавленный `object_id` — готовы.
- Старый `schedule_page.dart` и остальные экраны подрядчика: они в проде.
- Готовые блоки экрана и лента месяцев — приняты глазами 2 сентября.
- Мастер расстановки с выбором месяца — это S2.4, кнопка пока создаёт сразу.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — `S2.3` и дальше.
- `els-vault/00-home/журнал - изменения контракта API для фронта.md` — что
  фронт уже может звать по графику.
- `~/els-figma/окно-график-объекта_1182-232.png` — кадр, откуда переходим.
