---
этап: 10 — раздел «Графики» у прораба и админа
статус: в работе
дата: 2026-08-26
план: els-vault/00-home/план - раздел графики у прораба и админа.md
---

# Передача: фаза 1 собрана, остался пункт 1.7 — подмена входов

Всё закоммичено: `9ac68b0 feat(графики): участки у админа и бесконечная
подгрузка ленты`. Пункты 1.5 и 1.6 закрыты, из фазы 1 остаётся 1.7.

## Сделано и проверено

- **Участки у админа** — `view/schedule_divisions_screen.dart` поверх
  `bloc/schedule_divisions_bloc.dart`: номер в кружке, название, прораб,
  пилюля процента; пороги 90 / 70 в `ScheduleDivision.color`, те же, что на
  главной. Клик открывает ленту с фильтром «Участок» и тем же годом, блок
  ленты создаётся свой на каждый заход — возврат назад не тащит прошлый отбор.
- **Развилка по роли** — `view/schedule_section.dart`: `admin` → участки,
  `foreman` → сразу лента.
- **Подгрузка ленты** — слушатель в `view/schedules_screen.dart` шлёт
  `SchedulesNextPageRequested` за 300 px до конца, состояние берёт у блока.
  Подвал: спиннер при догрузке, «Это все объекты» при `hasNext == false` и
  `page > 1`, иначе ничего.
- **Пилюля года** вынесена в `widgets/schedule_year_picker.dart` — одна на раздел.
- **Проверки**: `flutter test` целиком — **296 тестов, все проходят** (11
  новых). `flutter analyze` по новым каталогам раздела — 0 замечаний (по
  разделу целиком 80, все в старых файлах подрядчика).
  Глазами через `flutter build web --release -t lib/main_schedules.dart` +
  `python3 -m http.server` на 1440 и 375: цвета по порогам верные, путь
  «участки → объекты → назад» проходит. Flutter: `~/development/flutter/bin`.

## Не доделано

- **Прокрутка ленты живьём не проверена.** В браузерном пейне до канваса не
  доходит ни колесо, ни перетаскивание, ни `Page Down` — то же ограничение
  автоматизации, что и с кликом по клетке. Поведение закрыто четырьмя
  тестами в `test/schedule/schedules_infinite_scroll_test.dart`.
- **Кадр `3:339` не сверен.** Figma REST отдаёт 429 с `retry-after` 160851 с
  (до 28 августа), тариф `starter`, лимит `low` — не проходит даже опись
  `depth=1`. Экран свёрстан по описанию плана. Обход — экспорт из самой
  Figma: `File → Export frames to PDF` в `~/Documents/els-макет/`.
- Белые цифры на светло-зелёной пилюле контрастят слабо. Так же сделана
  клетка «выполнено» — менять надо либо обе, либо ни одной.
- `make lint` и `make test` не гонялись: обе про бэкенд, к фронту не относятся.

## Следующий этап

**Цель.** Пункт 1.7 — закрыть фазу 1: новый раздел встаёт на место старого.

**Готово, когда.** Оба входа ведут в новый раздел, старые
`schedule_screen.dart` и `schedule_screen_foreman.dart` удалены,
`flutter test` зелёный, экран пройден руками на вебе.

## Первые шаги

1. `home_page.dart:69` и `home_foreman.dart:55` — переход на
   `ScheduleSection(role: …)`.
2. `ScheduleSection` принимает `repository`, но по умолчанию внутри блоков
   стоит `FixtureSchedulesRepository` — в проде это выдуманные объекты.
3. Виджет-тесты на `MonthStrip` — последний пункт чек-листа 1.7.

## Не трогать

- Бэкенд целиком: новые ручки, `ilike` и индексы — фаза 2.
- `schedule_page.dart` и `schedule_page_foreman.dart` — это карточка графика
  ТО, а не раздел «Графики».

## Уточнить перед стартом

- **Переключать ли входы, пока под разделом фикстура.** В проде новый экран
  покажет выдуманные объекты. Либо 1.7 ждёт фазу 2, либо входы меняются
  вместе с подключением сети. Решение человека.
- Токен Figma из переписки 25 августа до сих пор не отозван.

## Ссылки

- `els-vault/00-home/план - раздел графики у прораба и админа.md` — чек-лист
  1.7 и вся фаза 2.
- `frontend/test/schedule/` — восемь файлов, образец для новых тестов.
