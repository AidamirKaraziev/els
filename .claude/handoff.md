---
этап: 11 — закрытие ТО с карточки работы
статус: закрыт
дата: 2026-09-04
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: ТО закрывается, остальные действия S2.5 не начаты

Работа закоммичена целиком: `5a91b29`, рабочая копия чистая.

## Сделано и проверено

- **Кнопка «Завершить ТО» смонтирована** в `bottomNavigationBar` карточки
  работы (`schedule_work_card_screen.dart`). Размещение внизу утверждено по
  наброску: чек-лист ТО длинный, и действие не должно уезжать под прокрутку.
- **У закрытого ТО на месте кнопки строка** «ТО завершено дд.мм.гггг».
  Карточка перечитывает себя `WorkDetailsRefreshed` — экран не покидается.
- **Лента года и срок ближайшего ТО** на экране объекта обновляются сразу по
  ответу сервера: `onToFinished` шлёт `ScheduleObjectYearRequested(state.year)`.
- **Точка подмены запроса** — параметр `request` у `FinishTOButton`. До неё
  виджет ходил в сеть прямо из обработчика и проверить его было нечем.
- **Проверки**: `flutter test` — 398 зелёных (было 393 + пять новых в
  `test/schedule/schedule_finish_to_test.dart`); `flutter analyze` — вне
  легаси `els_mobile/` ошибок нет. Бэкенд не трогали, `make lint` и
  `make test` не гонялись.
- **Приёмка глазами**: `make up`, localhost:8080, 1280 px, под прорабом,
  объект «1аСпортмастер», 2027. Июльская клетка → кнопка → диалог →
  «Завершить»: пришли `SnackBar`, строка с датой и время окончания в блоке
  «Времена». Назад — клетка пожелтела, срок ближайшего ТО переехал с
  «ТО 1 · 1 – 31 июля» на «ТО 6 · 1 – 31 августа», без перезагрузки.

## Не доделано

- **Лента раздела «Графики» после закрытия не обновляется.** Отложено
  сознательно: единственное событие обновления там — `SchedulesRequested`, а
  оно сбрасывает постраничную загрузку к первой странице. Патчить клетку на
  клиенте нельзя — `MonthCell` намеренно не считает `done`/`late` в браузере
  (комментарий в `models/month_cell.dart`). Нужно точечное обновление строки.
- **Остальные действия S2.5 не начаты** — в этом этапе делали только закрытие
  ТО, по границам, которые поставил пользователь.
- **Под админом путь руками не проверен** — сессия в браузере прорабская,
  пароли агент не вводит. Класс тот же, отличается только роль.
- **Ручка создания вида ТО не заведена** (тянется с этапа 10):
  `types_acts.id` без автоинкремента и равен периодичности, есть только
  `GET /type-acts/`; схемы `TypeActCreate`/`TypeActUpdate` и `crud_type_acts`
  готовы, миграция не нужна. Открыт вопрос — откуда брать `id`.

## Данные стенда

Закрыты руками ради приёмки, откатывать не решили:

- шесть актов объекта 25 за 2027, `acts_fact.id` 309–314 (этап 10);
- июльский акт 2027 у «1аСпортмастер» — закрыт этой приёмкой, `finished_at`
  проставлен 04.09.2026.

Откат первых шести: `update acts_fact set finished_at = null, status_id = 1
where id between 309 and 314`.

## Следующий этап

**Цель.** Доделать оставшиеся пункты S2.5 — действия с ТО на новом экране
помимо закрытия.

**Готово, когда.** Пункты S2.5 в плане закрыты, `flutter test` зелёный,
приёмка глазами на `make up`.

## Первые шаги

1. Прочитать S2.5 в плане — он идёт **после** S2.6; в прошлой сессии из него
   сделан только первый пункт (закрытие ТО).
2. Решить с пользователем, входит ли обновление ленты раздела «Графики» в
   S2.5, и если да — заводить событие точечного обновления строки в
   `bloc/schedules_bloc.dart`, а не рефетч.
3. Проверять на «1аСпортмастер» за 2027: январь–июль закрыты, август–ноябрь нет.

## Не трогать

- Бэкенд: ручку создания вида ТО не заводим, пока не решён вопрос про `id`.
- Заглушки `SizedBox.shrink()` в `_screens` и `_screensForeman`:
  перенумеровать списки заманчиво, но это тронет два десятка переходов.
- Легаси `els_mobile/` — его ошибки анализа не наши.

## Уточнить перед стартом

- Входит ли обновление ленты «Графиков» в этот этап или идёт отдельной задачей.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — S2.5 и дальше.
- `frontend/lib/screns/schedule/view/schedule_work_card_screen.dart` — карточка
  работы с нижней панелью, точка входа для новых действий с ТО.
