---
этап: S5.2 — оформление внутреннего акта клиенту
статус: в работе
дата: 2026-09-06
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: экран оформления сверстан и принят, к ручкам не привязан

Закоммичено в `c58b104`, рабочая копия чистая. Макет утверждён, логика
выпуска не написана — это и есть остаток этапа.

## Сделано и проверено

- **Экран «Оформить клиенту»** в `foreman/defects/issue_to_client_screen.dart`:
  свой заголовок и описание (подсказками стоит текст механика), сетка
  снимков с галочками, кнопка «Сформировать PDF». Отмечены по умолчанию все
  снимки. Экран собирает `IssueDraft` и отдаёт наружу через `onIssue` — в
  сеть не ходит.
- **`IssueDraft.toBody()`** повторяет `DefectiveActIssueToClient`: пустые
  тексты не уходят вовсе (сервер подставит из внутреннего акта), а
  `photo_ids` уходит всегда, в том числе пустым списком.
- **Набросок живой**: `lib/dev/issue_to_client_preview.dart`, точка запуска
  `issue-to-client-preview` на порту 5607 в `.claude/launch.json`. Снял
  галочку, вписал заголовок — тело вышло
  `{client_title: …, photo_ids: [1, 3]}`. Вёрстка принята пользователем.
- **Решено по наброску**: кнопки «Оформить ещё один» нет — оформить заново
  можно той же кнопкой; клиентских актов из одного внутреннего может быть
  сколько угодно; PDF должен скачиваться.
- **Проверки**: `flutter test` — **465** (было 455, +7 новых в
  `test/foreman/issue_to_client_test.dart`), `flutter analyze` по затронутым
  файлам чист. `make lint` и `make test` не гонялись: бэкенд не менялся.

## Не доделано

- **Экран ни к чему не привязан.** В карточке дефекта нет кнопки «Оформить
  клиенту», блока «Оформлено клиенту» со ссылкой и PDF тоже нет.
- **Хвост S5.1 руками не прогнан.** Две точки входа механика — дефект с
  работы по ТО целиком и с пункта чек-листа — по-прежнему не видели на
  живых данных, «Вид ТО» в карточке не подтверждён. Данные под прогонку
  готовы, см. первый шаг.
- **Заглушка картинок врёт.** `helper/api_image.dart`: `_blank` описан как
  прозрачный пиксель, а декодируется в полупрозрачный синий — пустой путь
  рисуется синим квадратом. Заведена отдельная задача.

## Следующий этап

**Цель.** Довести S5.2: из внутреннего акта получается клиентский, PDF
скачивается, акт уходит в `issued`.

**Готово, когда** из акта на объекте 82 выходит клиентский с выбранными
фото и своим заголовком, PDF открывается с кириллицей, а родитель показан
как «Выдан клиенту» со ссылкой на потомка.

## Первые шаги

1. Сначала прогнать хвост S5.1. Данные готовы: механик `m@mail.ru` (id 2),
   объект **82 «Создаю Тест»** — 12 плановых ТО за 2025 год с видами ТО
   1/3/6/12 и чек-листами, сентябрьское — `acts_fact` 296. Объекту руками
   проставлен `is_actual=true`. Прораб тот же — `pr@mail.ru` (id 3). На
   объекте 80 работ по ТО нет вовсе, там сценарий недоступен.
2. Привязать экран: кнопка в `foreman/defects/defect_card_screen.dart` под
   блоками, `Navigator.push` на `IssueToClientScreen`, из `onIssue` —
   `POST /defective-act/{id}/issue-to-client/`, затем
   `POST /defective-act/{id}/pdf/` (обе живые,
   `backend/src/api/api_v1/endpoints/defective_act.py:234` и `:260`).
3. Скачивание PDF: путь приходит в `pdf_file`, статика требует токена —
   смотреть, как это делают выгрузки отчётов (`POST /files/link`), а не
   дописывать схему руками.

## Не трогать

- Ручку создания вида ТО; `status_id` и `PUT /defective-act/{id}/status/`.
- Легаси `els_mobile/`, заглушки `SizedBox.shrink()`, `soon_screen.dart`.
- Очередь в `mechanic/data/outbox.dart` и отложенный ключ.
- `IntTest.indexScreensForeman` и гигантский `build` в
  `object_page_foreman.dart` — только добавлять.

## Уточнить перед стартом

- Открывается ли клиентский акт отдельным экраном или в карточке хватит
  строки и PDF — на наброске нарисована ссылка, но вопрос не закрыт.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — раздел S5.2, строка 485.
- `els-vault/00-home/журнал - изменения контракта API для фронта.md` — ручки
  дефектных актов.
