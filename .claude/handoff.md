---
этап: 2 — каркас экрана графика объекта (S2.1)
статус: закрыт
дата: 2026-09-02
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: каркас экрана есть, дальше лента месяцев

Работа закоммичена: HEAD `056b7c1`. Не закоммичены галочки `S2.1` в плане
и эта передача.

## Сделано и проверено

- **Экран объекта живёт в `frontend/lib/screns/schedule/object/`**: модели,
  `ScheduleObjectRepository` + фикстура, `ScheduleObjectBloc`,
  `ScheduleObjectScreen`, четыре виджета. Три блока кадра `1182:232` —
  «Информация об объекте», «Местоположение», «Ответственные».
- **Смотрится без входа в систему**:
  `flutter run -t lib/dev/schedule_object_preview.dart -d chrome`. Файл в
  прод-сборку не попадает, на него ничто не ссылается.
- **Расхождения с кодом подрядчика, принятые по кадру**: первая строка —
  «Организация» (у подрядчика «Название»); плашек «Прораб удалён /
  заморожен» нет; кнопок зума на карте нет.
- **Строка «Телефон» добавлена вопреки кадру** — просьба пользователя от
  2 сентября: с экрана звонят. В комментарии к виджету это записано, чтобы
  строку не «починили» обратно по макету.
- **Карта перенесена как есть** (тот же `flutter_map`, те же тайлы), но
  координаты идут аргументом, а не из глобальной `listSelectedObject`:
  иначе её не показать ни на фикстуре, ни в тесте.
- **Узкая ширина**: ниже 1000 px колонки встают одна под другой, ниже
  620 px — плашки ответственных. Подпись роли больше не переносится:
  «Прораб» ломался пополам. Снято на 430 px и 1440 px.
- **`ScheduleRole` переехал** в `models/schedule_role.dart`, из
  `schedules_screen.dart` реэкспортирован — места встраивания и тесты видят
  его там же, где раньше.
- **Проверки этой сессии**: `flutter test` — 320 passed, `flutter analyze`
  по новым файлам чист. Бэкенд не трогали, `make test` не гоняли.

## Не доделано

- **В ленту экран не вшит** — сознательно. Старый `SchedulePage` остаётся
  точкой по умолчанию, пока новый не показывает график.
- **Виджет-тестов на новый экран нет** — фикстура готова, писать в `S2.2`.
- **Шапки оболочки нет** — на фикстуре её не поднять, ждёт встраивания.
- **Кадры `83-312`, `275-764`, `1826-283`** не выгружены. Квота Figma REST
  отпустит ~6 сентября, сам достать не могу.

## Следующий этап

**Цель.** `S2.2` — лента 12 месяцев на экране объекта: цвета состояний,
переключатель года, пустой год с кнопкой создания.

**Готово, когда.** Лента показывает состояния цветом, год переключается,
год без графика показывает пустую ленту и кнопку «Создать график на 2027».

## Первые шаги

1. Прочитать `S2.2` в плане: там же записано, чего в кадре нет — подписей на
   клетках и переключателя года. Эту часть рисуем наброском на утверждение.
2. Цвета брать из `frontend/lib/helper/class_colors.dart`, пять состояний;
   своих не заводить. Пилюля с текстом «ТО 1/3/6/12».
3. Расширять `ScheduleObjectCard` и фикстуру, а не заводить вторую модель:
   лента — часть той же карточки объекта.
4. Сеть подключается в конце `S2.2`: `GET /planned-to/` и
   `POST /planned-to/generate/` (тело: `object_id`, `year`) уже живые.

## Не трогать

- Бэкенд графика — этап 1 закрыт, `preview` и `generate` менять не нужно.
- Старый `schedule_page.dart` и остальные экраны подрядчика: они в проде.
- Три готовых блока экрана — приняты глазами 2 сентября.
- `ScheduleWorkCardScreen` и переход в карточку сотрудника — это `S2.3`.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — `S2.2` и дальше.
- `els-vault/00-home/журнал - изменения контракта API для фронта.md` — что
  фронт уже может звать по графику.
- `~/els-figma/окно-график-объекта_1182-232.png` — кадр, лента в нём есть.
