---
этап: 10 — раздел «Графики» у прораба и админа
статус: закрыт
дата: 2026-08-27
план: els-vault/00-home/план - раздел графики у прораба и админа.md
---

# Передача: раздел принят живым, приёмка пройдена обеими ролями

Работа сессии закоммичена: HEAD `7338c3d`.

## Сделано и проверено

- **Приёмка пройдена в браузере на боевых данных.** Все пункты блока
  «Проверка / Фронт» плана отмечены. Лента у админа открывается сразу,
  поиск уходит на сервер (`?search=` в ручке, а не резка загруженного),
  скролл догружает `page=2`, окно 1280 px не ломает колонки, прораб видит
  ленту без уровня участков.
- **Починена пустая карточка работы.** Клетка незакрытой работы открывала
  белый экран: `ScheduleWorkCardScreen` создавал блок с фазой ленты сданных,
  и на акте с пустым `finished_at` приходило `WorkGone`, которого экран не
  рисует. Введена фаза `WorkPhase.standalone` — карточка графика края списка
  не проверяет. Поймано на акте 300 (34 пункта чек-листа, экран пуст).
- **Починен сброс отбора при уходе в другой раздел.** Оболочка держит
  разделы одной позицией в дереве, и блок ленты умирал вместе с разделом.
  Блок переехал в оболочку у обеих ролей (`home_page.dart`,
  `home_foreman.dart`), `ScheduleSection` получает его параметром `bloc`.
  Проверено вживую: после «Задачи» → «Графики» блок тот же объект.
- **Проверки**: `make test` — **836 тестов**, `flutter test` — **314**
  (было 310), `make lint` чист, `flutter analyze` по тронутым файлам чист.
- **Правило про участки зафиксировано в плане**: прораб на чтение видит все
  участки (`core/access.py`, `ScopeKind.ALL`) — «видит всё, меняет только
  своё». Прежняя формулировка про «чужих участков не видно» была неверной.

## Не доделано

- **`CREATE EXTENSION pg_trgm` требует прав суперпользователя.** Локально
  они есть, у прод-роли — вопрос открытый. Выяснить до `make prod-deploy`,
  иначе миграция `b6e4a9d31c05` упадёт на первой строке.
- **Раздел «Объекты» у админа показывает «Объектов нет»** при 47 объектах в
  базе. К «Графикам» отношения не имеет, не трогал — отдельный заход.
- **Старые `schedule_screen*.dart` и мёртвая `getAllTOGraphics`**
  (`schedule_page.dart:281`) — за ними вход в редактор ТО, отдельный заход.
- Журнал `изменения контракта API для фронта` про удаление
  `GET /schedules/divisions` по-прежнему молчит.

## Следующий этап

**Цель.** Раздел «Графики» доехал до прода вместе с миграцией поиска.

**Готово, когда.** `make prod-deploy` прошёл, миграция `b6e4a9d31c05`
накатилась, лента и поиск работают на боевом домене.

## Первые шаги

1. Выяснить права прод-роли на `CREATE EXTENSION` — от ответа зависит,
   катится ли `b6e4a9d31c05` как есть или её надо переписывать.
2. Дописать в `els-vault/00-home/журнал - изменения контракта API для
   фронта.md` удаление `GET /schedules/divisions` и фильтр
   `without_division`.
3. `make lint`, `make test`, `flutter test` — и только потом выкат.

## Не трогать

- `schedule_page.dart` и `schedule_page_foreman.dart` — редактор графика ТО.
- Контракт живых ручек: новое — только новые пути либо необязательные
  параметры.
- Правила доступа в `core/access.py`: «прораб видит всё» — решение
  заказчика, менять только по отдельной просьбе.

## Уточнить перед стартом

- Катим ли на прод в эту сессию или сперва закрываем «Объектов нет».

## Ссылки

- `els-vault/00-home/план - раздел графики у прораба и админа.md` — блок
  приёмки с отметками, что именно проверено.
- `frontend/test/schedule/` — тесты раздела, включая два новых на карточку
  незакрытой работы и на сохранение отбора.
