---
этап: 5 — мастер расстановки графика (S2.4), живые ручки
статус: в работе
дата: 2026-09-02
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: вид мастера принят и закоммичен, логика не начата

Правки вида лежат в `9eab4fb` поверх наброска `0978c04`. Рабочая копия чистая.
Часть 2 (живые `preview`/`generate`) не начиналась — ни одного файла.

## Сделано и проверено

- **Лента предпросмотра перекрашена**: «добавится» — светло-серый,
  «уже занято» — белый, «нет шаблона» — янтарный (`myColorYellowLight` в
  рамке `myColorYellow`). Громкий цвет на ленте ровно один.
- **Старт цикла виден**: клетка с `position == 1` чёрная, с зелёным тегом
  «СТАРТ». Место под тег резервируют все клетки — ряд не идёт волной.
- **Старт на клетке без шаблона** сохраняет янтарную заливку и берёт тёмную
  рамку 2px: предупреждение важнее, из-за него график не утверждается.
- **Вид принят пользователем глазами** 2 сентября на dev-точке входа
  `lib/dev/schedule_wizard_preview.dart`. Поднимать её надо готовой
  конфигурацией `schedule-wizard-preview` из `.claude/launch.json` (порт 5600,
  `cwd: frontend`) — своих обёрток не заводить.
- **Проверки этой сессии**: `flutter test test/schedule/` — 90 passed,
  `flutter analyze` по `wizard/` и `class_colors.dart` — чисто.
  `make lint` и `make test` не гонялись: бэкенд не трогали.

## Не доделано

- **Тестов на новый вид нет.** Ни на тег «СТАРТ», ни на случай «старт на
  клетке без шаблона» — последний на фикстурах не воспроизводится, позиция 1
  всегда ТО 1, у которого шаблон есть. Нужен тест на собранных вручную клетках.
- **Фикстура не двигает старт.** `buildWizardFixture` строит клетки с зашитым
  `_anchorMonth = 3`, шаг 2 меняет только текст шапки: она говорит «с января»,
  а СТАРТ стоит на марте. Уйдёт с живым `preview`, отдельно чинить не надо.
- **Мастер по-прежнему на фикстуре**: ручки не зовутся, `anchor_month` никуда
  не уходит, «Утвердить» закрывает мастер с `true`, а расстановку делает
  прежнее `ScheduleObjectGenerateRequested`.
- **Кнопка «Создать шаблон» выключена** — решено сознательно, см. ниже.

## Следующий этап

**Цель.** Посадить мастер на `GET /planned-to/preview/` и
`POST /planned-to/generate/`, передавая выбранный на шаге 2 `anchor_month`.

**Готово, когда.** Прораб проходит три шага на живых данных и график
появляется в базе; при клетке «нет шаблона» «Утвердить» не нажимается.

## Первые шаги

1. Открыть готовый план части 2:
   `/Users/ajdamirkaraziev/.claude/plans/abundant-drifting-rose.md` — там
   разобраны контракт репозитория, ветка 422 и правка экрана объекта.
2. `anchor_month` у `preview` **необязательный**
   (`backend/src/api/api_v1/endpoints/schedule_plan.py:198`). Порядок: зовём без
   якоря → 200 даёт `anchor_month` прошлого года и шаг 2 отпадает; 422 (`num: 144`)
   → повторяем с `anchorMonth: 1` и показываем шаг 2, смена месяца → перезапрос.
3. Экран объекта на `true` должен перечитывать год, а не слать
   `ScheduleObjectGenerateRequested` (`schedule_object_screen.dart:189`), иначе
   расстановка пойдёт дважды. Старое событие и `generateYear` не удалять —
   пометить `@Deprecated`, они в проде.

## Не трогать

- Бэкенд графика: `preview`, `generate`, `object_id` у `/schedules/rows`.
- `schedule_page.dart` и прочие экраны подрядчика — они в проде.
- Готовые блоки экрана объекта, лента месяцев и оба перехода — приняты 2 сентября.
- Права и роли: `USER_READ`, `canOpenEmployeeCard`. Отдельная задача.
- Программу модели: `PUT /maintenance-program/by-model/{id}/` не зовём.
- **Кнопка «Создать шаблон» остаётся выключенной.** Решено 2 сентября: экран
  шаблонов чек-листа делаем свой (не переиспользуем `TO/liftMO.dart` и
  соседей), но отдельным этапом — с макетом и ручками шаблонов.

## Ссылки

- `frontend/lib/screns/schedule/object/wizard/` — весь мастер: модели, шаги, вид.
- `backend/src/api/api_v1/endpoints/schedule_plan.py` — обе ручки и коды ошибок.
- `frontend/lib/screns/schedule/object/repository/api_schedule_object_repository.dart`
  — образец для репозитория мастера: конверт, ошибки, подменяемые `_send`.
