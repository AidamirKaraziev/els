---
этап: 4 — лента сданных работ у прораба
статус: в работе
дата: 2026-08-19
план: els-vault/00-home/план - мобильное приложение механика и замыкание процесса.md
---

# Передача: лента и выход из аккаунта закоммичены, но сквозной прогон не сделан

Всё в коммите `7842429`; рабочая копия чистая, кроме чужого `frontend/els_mobile/`.

## Сделано и проверено

- **Проверки гонялись в этой сессии:** `make lint` чист, `make test` — **761**,
  `flutter test` по `test/profile_nav_test.dart`,
  `test/avatar_opens_profile_test.dart`, `test/mechanic`,
  `test/submitted_works` — **88**, `flutter analyze` без ошибок.
- **Лента прораба открыта вживую** на http://localhost:8080: число на пункте
  меню сходилось с базой, четыре строки, бейджи, «Проверил» чинит строку и
  гасит счётчик, отбор «только непросмотренные» работает.
- **Профиль прораба проверен вживую:** аватарка ведёт в «Мой Профиль», «назад»
  вернула на «Сданные работы» — раздел, с которого уходили, «Выйти» увела на
  «Авторизацию» и очистила `localStorage`.
- Заявка со статусом «Проблема» попадает в ленту и в счётчик — покрыто тестами
  бэка, но **вживую бейджа никто не видел**.

## Не доделано

- **Сквозной прогон этапа 4 не сделан:** механик закрывает заявку «Проблемой» →
  строка у прораба с бейджем → число растёт → «Проверил» гасит.
- **В базе не на чем проверять счётчик:** все 4 сданные заявки уже отмечены,
  непросмотренных **0**. Свежую строку даст только механик.
- **Профиль админа и диспетчера и «назад» у механика вживую не открывались** —
  код общий с прорабским, но глазами не смотрели.
- **Вид профиля с макетом не сверялся:** Figma отдавала `429` больше часа.
  Кадры `275:764` (админ), `1826:283` (механик).
- **`FIGMA_TOKEN` засветился в выводе сессии** — по заметке про интеграцию его
  полагается перевыпустить.

## Следующий этап

**Цель.** Закрыть этап 4 сквозным прогоном, затем этап 3 — ТО с чек-листом.

**Готово, когда.** Механик закрыл заявку «Проблемой» в телефоне → строка
появилась у прораба с бейджем, число выросло → «Проверил» число погасил, и это
держится после перезагрузки страницы.

## Первые шаги

1. `make up`, вход механиком `m@mail.ru` — пароль вводит человек. У него открыты
   заявки `55`, `56` (в работе) и `58`, `61` (новые), кнопка «Проблема» —
   `frontend/lib/mechanic/screens/order_screen.dart:192`.
2. Дальше вход прорабом `pr@mail.ru`; заодно посмотреть стрелку «назад» на
   вкладке «Личный кабинет» у механика.
3. **Осторожно с браузерным управлением:** кадр шириной 800 с чёрными полями,
   а страница — 697; клики по координатам кадра уходят мимо и жмут чужие
   кнопки. Дерево доступности Flutter не включать — оно перехватывает клики.
4. Фронт — контейнером `ghcr.io/cirruslabs/flutter:stable`, `flutter pub get` в
   той же команде; после него откатывать `frontend/pubspec.lock` — контейнер
   понижает версии транзитивных пакетов.

## Не трогать

- `frontend/android/`, iOS-сборку, `frontend/els_mobile/`, сторожа `if (kIsWeb)`
  вокруг выбора фото.
- `frontend/test/widget_test.dart` — шаблон подрядчика, падает всегда, из-за
  него полный `flutter test` красный.
- Выкат на прод и дыру со статикой в nginx — решение отложено.
- Экран профиля у владельца (роль «клиент») — договорились не заводить.

## Уточнить перед стартом

- Сверять ли профиль с макетом Figma, когда отпустит лимит: вёрстка кнопки
  «Выйти» может поменяться.
- Перевыпущен ли `FIGMA_TOKEN`.

## Ссылки

- `els-vault/knowledge/integrations/макет достаётся из Figma по REST, токен в окружении.md` — как забрать кадр.
- `els-vault/knowledge/decisions/фронт правится по макету, а не по коду подрядчика.md` — порядок источников по виду.
