---
этап: 10 — чистка: экраны подрядчика сняты, значки «?» расставлены
статус: закрыт
дата: 2026-09-04
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: S2.6 закрыт, следующий на очереди S2.5

Ничего не закоммичено: вся работа в рабочей копии поверх `27a56f0`.

## Сделано и проверено

- **Экраны подрядчика сняты — 15 750 строк.** Удалены `schedule_page.dart`,
  `schedule_screen.dart`, обе `*_foreman`, весь `lib/TO/` (четыре экрана
  выполнения ТО), `test_window.dart` и мёртвая `getAllTOGraphics`.
- **Слоты в оболочках — заглушки.** В `_screens` и `_screensForeman` на месте
  снятых экранов `const SizedBox.shrink()`: нумерация позиционная, и вынуть
  элемент нельзя, не сдвинув два десятка переходов.
- **Значки «?».** `scheduleMonthCreatesAct` у подписи «Плановые ТО» в
  `object_schedule_card.dart`, `scheduleFinishTo` в шапке
  `schedule_work_card_screen.dart` (у блока времён нельзя: `WorkTimesBlock`
  общий с заявками). Нажатие покрыто `test/schedule/schedule_hints_test.dart`.
- **Чек-листы ТО сохранены** в `knowledge/business/чек-листы ТО от
  подрядчика.md` — 556 пунктов, вбитых руками в `lib/TO/`.
- **Проверки**: `flutter analyze` — вне легаси `els_mobile/` ноль ошибок;
  `flutter test` — 393 зелёных (было 391 + два новых). Бэкенд не трогали,
  `make lint` и `make test` не гонялись.
- **Приёмка S2.6 глазами**, `make up`, localhost:8080, под прорабом: все пять
  случаев прошли — объект без графика; половина ТО выполнена; модель без
  шаблонов (мастер не даёт утвердить); год 2026 → 2025 → 2027 и возврат с
  карточки в тот же год; окно 1280 px. Пункты S2.6 в плане закрыты.

## Не доделано

- **Кнопки «Завершить ТО» нет.** `finish_to_button.dart` написан вместе с
  запросом `finishFactAct`, но никуда не смонтирован, а подсказка «Как закрыть
  ТО» её уже обещает — решено оставить и довести кнопку в S2.5.
- **Данные стенда изменены под приёмку**: закрыты шесть актов объекта 25 за
  2027 (`acts_fact.id` 309–314). Откат — `update acts_fact set finished_at =
  null, status_id = 1 where id between 309 and 314`.
- **Под админом путь руками не проверен** — сессия в браузере прорабская,
  пароли агент не вводит. Класс тот же, отличается только роль.
- **Ручка создания вида ТО не заведена.** `types_acts.id` без автоинкремента
  и равен периодичности, есть только `GET /type-acts/`; схемы
  `TypeActCreate`/`TypeActUpdate` и `crud_type_acts` готовы, миграция не нужна.
  Открыт вопрос — откуда брать `id`.

## Следующий этап

**Цель.** S2.5: довести действия с ТО на новом экране, начиная с закрытия ТО.

**Готово, когда.** Прораб закрывает ТО из карточки работы, лента и срок
ближайшего ТО обновляются без перезагрузки, `flutter test` зелёный.

## Первые шаги

1. Прочитать S2.5 в плане — он идёт в файле **после** S2.6.
2. Смонтировать `FinishToButton` в `schedule_work_card_screen.dart`: запрос
   `finishFactAct` (PUT `/api/v1/act-fact/{id}/`, `status_id = 4`) готов, нет
   места на экране и обновления состояния после ответа.
3. Проверять на объекте 25 за 2027: январь–июнь закрыты, июль–ноябрь нет.

## Не трогать

- Бэкенд: ручку создания вида ТО не заводим, пока не решён вопрос про `id`.
  Легаси `els_mobile/` — его ошибки анализа не наши.
- Заглушки `SizedBox.shrink()` в `_screens`: перенумеровать списки заманчиво,
  но это тронет два десятка переходов в чужом коде.

## Уточнить перед стартом

- Где на карточке работы стоит кнопка «Завершить ТО» — кадра нет, нужен
  набросок на утверждение.
- Данные стенда откатывать или оставить закрытыми шесть актов.

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — S2.5 и дальше.
- `frontend/lib/screns/schedule/widgets/finish_to_button.dart` — готовая кнопка.
