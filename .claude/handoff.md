---
этап: 10 — раздел «Графики» у прораба и админа
статус: в работе
дата: 2026-08-26
план: els-vault/00-home/план - раздел графики у прораба и админа.md
---

# Передача: отбор работает, дальше — участки и подгрузка

Всё закоммичено: `c05af52 feat(графики): панель фильтров и поиск над лентой
объектов`. Рабочая копия чистая. Пункты 1.3 и 1.4 плана закрыты.

## Сделано и проверено

- **Панель над списком**: год, пять выпадающих фильтров (Название, Заводской
  номер, Участок, Тип, Графики) и поиск по лупе —
  `frontend/lib/screns/schedule/widgets/schedule_filters_bar.dart`. Отбор
  применяется сразу при выборе, активная пилюля зелёная, со значением и
  крестиком; «Сбросить всё» появляется от двух условий и год не трогает.
- **Поиск** — `widgets/schedule_search_field.dart`: debounce 300 мс, крестик
  отменяет и отложенный запрос. Складывается с фильтрами через
  `ScheduleFilters.search`, фикстура это уже умеет.
- **Отбор живёт в состоянии блока**: `filters` и `options` — в базовом
  `SchedulesState`, по образцу `WorksReportState` экрана отчётов. Значения
  выпадающих просятся один раз, `SchedulesFilterOptionsRequested`.
- **Пустая выдача** различает «объектов нет» и «под отбор никто не подошёл»:
  во втором случае — число условий и кнопка «Сбросить всё».
- **Проверки**: `flutter analyze` — 0 замечаний, `flutter test` — **285
  тестов, все проходят** (14 новых). Глазами на 1440 / 960 / 760 / 375 через
  `flutter build web --release -t lib/main_schedules.dart` + `python3 -m
  http.server` из `build/web`. Локальный Flutter: `~/development/flutter/bin`.

## Не доделано

- **Клик по клетке живьём так и не проверен.** Синтетическим `PointerEvent`
  выпадающий список открывается, но внутри ленты жест перехватывает прокрутка.
  Ограничение автоматизации, не поломка; закрыто тестом
  `test/schedule/schedules_screen_open_card_test.dart`.
- **Кадр `83:312` не сверен**: Figma отдавала `429 Rate limit exceeded` восемь
  попыток подряд. Пилюли свёрстаны по разбору из прошлой передачи (24 px,
  `fs=10`, стрелка справа) — сверить размеры, когда лимит отпустит.
- `make lint` и `make test` не гонялись: обе про бэкенд, к фронту не относятся.
- Пункты 1.5, 1.6 и 1.7 плана — по договорённости вне сессии.

## Следующий этап

**Цель.** Уровень участков у админа и бесконечная подгрузка ленты — пункты
1.5 и 1.6 плана.

**Готово, когда.** Админ попадает сначала на список участков (кружок с
номером, название, прораб, пилюля с процентом), клик по участку открывает
объекты с фильтром «Участок»; прораб этот экран не видит. Лента догружает
следующую страницу при подходе к концу, «больше нет» — из `hasNext`.

## Первые шаги

1. Подгрузка дешевле: `_scrollController` в `view/schedules_screen.dart`
   создан, но слушателя нет. Повесить и слать `SchedulesNextPageRequested` —
   блок это событие **уже обрабатывает**, со склейкой строк и `_requestId`.
2. Экран участков по кадру `3:339`. Данные — `fetchDivisions(year:)`, фикстура
   отдаёт пять участков с процентами 82/63/49/94/71. Пороги 90 % и 70 % взять
   те же, что у карточки «Выполнение графика» на главной.
3. Роль — параметр `ScheduleRole` в `SchedulesScreen`: он есть, но ни на что
   не влияет. Участки показывать только `admin`.

## Не трогать

- `schedule_screen.dart`, `schedule_screen_foreman.dart`, `schedule_page.dart`,
  `schedule_page_foreman.dart` — живут до конца фазы 1.
- Бэкенд целиком: новые ручки, `ilike` и индексы — фаза 2.
- `main.dart`, `home_page.dart:69`, `home_foreman.dart:55` — подмена входов
  это пункт 1.7, в конце фазы.

## Уточнить перед стартом

- **Переключать ли входы раньше срока.** `make up` показывает старый экран
  подрядчика, и это уже вызвало вопрос. Новый пока сидит на фикстуре — в проде
  покажет выдуманные объекты. Решение человека.
- Токен Figma, засветившийся в переписке 25 августа, отзывать пока не стали —
  он рабочий. Висит с той сессии.

## Ссылки

- `els-vault/00-home/план - раздел графики у прораба и админа.md` — чек-лист
  фазы 1, пункты 1.5–1.7.
- `frontend/test/schedule/` — шесть файлов, 24 теста, образец для новых.
