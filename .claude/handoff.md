---
этап: 1 — программа обслуживания (S1.1)
статус: закрыт
дата: 2026-09-01
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: сущность программы в базе, дальше ручки

Работа закоммичена: HEAD `ca01030`. Рабочая копия чиста.

## Сделано и проверено

- **`S1.1` закрыт.** `backend/src/models/maintenance_program.py`: программа
  одна на модель оборудования (`factory_model_id` уникален) плюс двенадцать
  позиций «месяц → вид ТО» отдельной таблицей со ссылкой на `types_acts`.
- **Ограничения держит база, а не код**: уникальность `(program_id, position)`,
  `CHECK position BETWEEN 1 AND 12`, `RESTRICT` на виде ТО, каскад на позициях.
- **Миграция `c7f5b2e84a19`** поверх `b6e4a9d31c05`, бэкфилла нет.
  `upgrade head` → `downgrade -1` → `upgrade head` прогнаны на тестовой базе
  в docker, обе стороны чистые.
- **Права** `MAINTENANCE_PROGRAM_READ` / `_WRITE` в `core/permissions.py`:
  чтение ушло в `_EMPLOYEE_READ` (там же `PLANNED_TO_READ`), запись — прорабу;
  админ получает всё через `frozenset(Permission)`.
- **Тесты**: `tests/test_models_maintenance_program.py` (5 сценариев на
  ограничения) и два теста прав в `tests/test_permissions_and_scope.py`.
- **Проверки**: `make lint` чист, `make test` — 845 passed.

## Не доделано

- **Ручек нет вовсе** — это `S1.2`, сознательно вне границ этапа.
  Программу пока можно завести только SQL-ом или из шелла.
- **Три кадра ждут ручной выгрузки** в `~/els-figma/`, PNG 2x:
  `лента-графиков_83-312.png`, `личный-кабинет-админ_275-764.png`,
  `личный-кабинет-механик_1826-283.png`. Квота Figma REST отпустит ~6 сентября.
  На `S1.2` не влияет — этап бэкендовый.

## Следующий этап

**Цель.** `S1.2` — ручки программы: чтение, upsert всех двенадцати позиций
одним телом, список со страницами и предложение по умолчанию.

**Готово, когда.** Программу модели можно прочитать, сохранить и получить
предложение по умолчанию; ручки описаны в OpenAPI, `make test` зелёный.

## Первые шаги

1. Прочитать `S1.2` в плане — там пути ручек, правило предложения по
   умолчанию (k % 12 → ТО12, k % 6 → ТО6, k % 3 → ТО3, иначе ТО1) и
   требование резать предложение по существующим `acts_bases` модели.
2. Роутер регистрируется в `backend/src/api/api_v1/api.py`; образец ручки с
   правами — любой эндпоинт с `Depends(require(Permission...))`.
3. Валидация: ровно 12 позиций без пропусков, существующий `type_act_id`,
   ошибка — 422 со списком проблем, не 500.
4. `make lint` и `make test`. Тест `test_every_route_declares_access.py`
   упадёт, если у новой ручки не объявлен доступ.
5. Дописать `els-vault/knowledge/.../журнал - изменения контракта API для фронта`.

## Не трогать

- `planned_to` — двенадцать колонок месяцев остаются как есть.
- Контракт живых ручек: новое — только новые пути либо необязательные параметры.
- `core/access.py`: «прораб читает всё» — решение заказчика.
- Фронт. Экран графика начинается с `S2.1`.

## Уточнить перед стартом

- Предложение по умолчанию отдаётся отдельной ручкой или полем в ответе
  `GET .../by-model/{id}/`, когда программы ещё нет?

## Ссылки

- `els-vault/00-home/план - окно график объекта.md` — `S1.2` и дальше весь план.
- `backend/src/models/maintenance_program.py` — поля и ограничения из `S1.1`.
- `~/els-figma/README.md` — ключи кадров, что уже выгружено.
