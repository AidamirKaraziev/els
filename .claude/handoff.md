---
этап: 9 — чистка и переезд наброска программы в бой
статус: в работе
дата: 2026-09-03
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: переезд дописан и закоммичен, приёмки глазами не было

Всё в коммите `28fffcd`, рабочая копия чистая. Осталась одна вещь —
посмотреть на живой точке 5602 и убрать набросок.

## Сделано и проверено

- **Предпросмотр переехал целиком**: строка программы над лентой (два
  состояния — «Изменить программу» и красное «Создать программу»),
  перетаскивание клеток со сдвигом всей цепочки, плашка «программы нет»,
  вернувшаяся `_KnownAnchorNote`. `data` стал nullable.
- **Окно правки программы в бою** — `wizard/widgets/wizard_program_dialog.dart`
  поверх `by-model` / `.../suggestion/` / `/type-acts/`: грузит само,
  показывает загрузку и ошибку с «Повторить», позиции переставляются за ручку.
  Вид ТО, заведённый в окне, помечен красным и гасит «Сохранить».
- **Мастер и экран объекта прокинуты**: `programRepository`, `modelId`,
  `modelName`; `modelId` берётся из `state.card.modelId`. Поправлены четыре
  точки `lib/dev/` — в `schedule-wizard-live` добавлено поле «ID модели».
- **Починен сброс якоря**: после сохранения программы блок перезапрашивал
  заготовку без якоря, и выбранный вручную месяц уезжал на январь. Теперь
  месяц переживает правку, на это есть тест.
- **Проверки этой сессии**: `flutter analyze` — 0 ошибок в затронутом (было
  14); оставшиеся 1368 — легаси `els_mobile/`, не наши. `flutter test` — 391
  тест зелёный (было 380), `make lint` чист, `make test` — 919 тестов бэка.
  Flutter локально есть: `~/development/flutter/bin/flutter`, 3.47.0.

## Не доделано

- **Руками ничего не проверено.** На живой точке 5602 экран не открывали:
  сохраняется ли правка программы и перерисовываются ли клетки года — знает
  только автоматика.
- **Набросок `lib/dev/draft/` и точка 5603 на месте** — снимаются после
  приёмки глазами, так решили в этой сессии.
- В базе у `planned_to` 87 (объект 25, 2027) освобождён декабрь: вернуть
  `december_to_id = 320`. Правка не наша, шаг ручной.

## Следующий этап

**Цель.** Принять переезд глазами на боевых данных и убрать набросок.

**Готово, когда.** На точке 5602 у объекта с моделью правка программы
сохраняется, клетки года перерисовываются, перетаскивание ТО сдвигает цикл;
после этого `lib/dev/draft/` и точка 5603 удалены, `flutter analyze` и
`flutter test` по-прежнему чисты.

## Первые шаги

1. Поднять стек: `make up` из корня, затем точка `schedule-object-live`
   (5602, `--dart-define=API_ORIGIN=http://localhost:8080`). Объект 25, год
   2027 — у него в карточке есть `factory_model_id`.
2. Пройти путь: «Создать график» → строка программы → «Изменить программу» →
   переставить позицию → «Сохранить» → убедиться, что клетки года сменились,
   а месяц начала цикла остался прежним.
3. Отдельно проверить модель **без** программы: строка красная, «Утвердить»
   выключена, окно открывается на создание с раскладкой от `/suggestion/`.
4. После приёмки удалить `frontend/lib/dev/draft/` (три файла),
   `frontend/lib/dev/schedule_program_draft.dart` и запись
   `schedule-program-draft` из `.claude/launch.json`.

## Не трогать

- `schedule_page.dart`, `schedule_page_foreman.dart` и прочие экраны
  подрядчика — они в проде.
- Бэкенд целиком: ручки готовы, ручку на создание вида ТО не заводим.
- Легаси `els_mobile/` — его 1368 ошибок анализа не наши.

## Уточнить перед стартом

- Что делать с видом ТО, который заводят в окне: оставить как есть (гасит
  «Сохранить») или заводить ручку создания в справочнике — это уже за
  границей этапа 9.

## Ссылки

- `frontend/lib/dev/draft/` — утверждённый набросок, сверять вид после переезда.
- `~/.claude/plans/sharded-soaring-pizza.md` — план этапа, части B–D закрыты.
- `.claude/launch.json` — точки 5602 (живая) и 5603 (набросок).
