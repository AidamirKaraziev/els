---
этап: 6 — переходы графика объекта (S2.3) и перетаскивание ТО
статус: в работе
дата: 2026-09-02
план: els-vault/00-home/план - окно график объекта.md
---

# Передача: S2.3 закрыт кодом, перетаскивание переезжает в мастер

**Ничего не закоммичено** — вся работа сессии лежит в рабочей копии поверх
`02435d1`. Изменены 10 файлов, новых два: `frontend/lib/dev/schedule_object_live.dart`
и `frontend/test/schedule/object_schedule_move_test.dart`.

## Сделано и проверено

- **Переходы S2.3 уже были написаны** попутно в S2.1 и S2.4 — `_openWork` и
  `openEmployeeCard` в `schedule_object_screen.dart`, четыре теста в
  `object_schedule_navigation_test.dart`. В плане не отмечены.
- **Оба перехода прошли живьём** на новой dev-точке `schedule-object-live`
  (порт 5602, `API_ORIGIN=http://localhost:8080`): человек подтвердил, что
  работает. Право `USER_READ` у прораба вопросов не вызвало.
- **Строка срока справа от ленты**: «ТО 1 · 1 – 31 мая» со значком календаря
  — ближайшее незакрытое ТО года, сначала самое раннее просроченное, потом
  назначенное. Всё закрыто или год пуст — строки нет. Точного планового
  интервала из кадра (`11 января – 17 января`) в данных нет: у работы только
  фактические `started_at`/`finished_at`, плановый срок — месяц целиком.
- **Перетаскивание ТО по месяцам в ленте** — сделано целиком: `Draggable` /
  `DragTarget` в `month_strip.dart` (`affinity: Axis.horizontal`, курсор-рука),
  событие `ScheduleObjectCellMoved`, `moveCell` в интерфейсе и обеих
  реализациях. Бэкенд не менялся: месяц ТО — колонка `<месяц>_to_id` в
  `planned_to`, `PUT /planned-to/{id}/` обновляет только присланные поля.
- **Проверки этой сессии**: `make lint` чист, `make test` — **919 passed**,
  `flutter test test/schedule/` — **123 passed** (было 116), `flutter analyze`
  по `lib/screns/schedule/object`, `lib/screns/schedule/widgets`, `lib/dev`,
  `test/schedule` — чисто (одна старая `info` в чужом
  `schedule_section_keeps_filters_test.dart:63`).

## Не доделано

- **Перетаскивание в ленте живьём не подтверждено.** Человек пробовал на
  5602, но попадал в мастер (год 2028 пуст) и тащил клетки предпросмотра.
  В ленте после этого не проверяли — и уже не надо, см. решение ниже.
- **Отметки в плане не проставлены**: пункты S2.3 в
  `план - окно график объекта.md` всё ещё пустые, расхождения с кадром там
  не записаны (отдельный экран вместо аккордеона, экраны сотрудника
  подрядчика как есть).
- В локальной базе освобождён декабрь у `planned_to` 87 (объект 25, 2027) —
  делали ради цели для переноса. Вернуть: `december_to_id = 320`.

## Следующий этап

**Цель.** Перетаскивание живёт в мастере расстановки, а не в ленте графика.

**Готово, когда.** На шаге «Предпросмотр» позиции ТО таскаются между
месяцами до нажатия «Утвердить», в базу уходит выбранная раскладка; в блоке
«Техническое обслуживание» перетаскивания нет вовсе.

## Первые шаги

1. Убрать перетаскивание из ленты: `onCellMoved` в
   `frontend/lib/screns/schedule/widgets/month_strip.dart` и
   `object_schedule_card.dart`, событие `ScheduleObjectCellMoved`, `moveCell`
   в трёх файлах `object/repository/`, тесты группы «перенос ТО
   перетаскиванием» в `test/schedule/object_schedule_move_test.dart`
   (группу «срок ближайшего ТО» из того же файла **оставить**).
2. Добавить перетаскивание в `object/wizard/widgets/wizard_preview_step.dart`
   — там раскладка ещё не в базе, и правка ничего не портит.
3. Год для проверки брать пустой: объект 25, 2028 — мастер откроется сам.

## Не трогать

- `schedule_page.dart`, `schedule_page_foreman.dart` и прочие экраны
  подрядчика — они в проде.
- Права и роли на бэке, программа модели (`PUT /maintenance-program/by-model/{id}/`).
- Кнопка «Создать шаблон» в мастере остаётся выключенной.
- Контракт `GET /schedules/rows` — новых полей не добавляли и не надо.

## Уточнить перед стартом

- **Нужен ли отдельный шаг «Программа модели» в мастере.** Сейчас это
  витрина: двенадцать позиций и предупреждение, правки нет (отложена
  2 сентября). Варианты: вернуть правку и сохранение, свернуть в строку на
  предпросмотре, оставить как есть. Мнение агента — свернуть.
- Переносить ли в ленте будущие ТО хоть как-нибудь после S2.6: старый экран
  подрядчика с «+» в клетке удаляется, и другого способа подвинуть один
  месяц не останется. Просроченные двигать нельзя — это стирало бы просрочку.

## Ссылки

- `frontend/lib/screns/schedule/widgets/month_strip.dart` — лента и весь код перетаскивания.
- `frontend/lib/dev/schedule_object_live.dart` — экран объекта на живом бэкенде, точка 5602.
- `frontend/lib/screns/schedule/object/wizard/` — мастер, куда перетаскивание переезжает.
