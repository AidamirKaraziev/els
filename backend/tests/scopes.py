"""Готовые области видимости для тестов.

Нужны там, где тест проверяет не доступ, а что-то другое — арифметику
статистики, отметки времени у заявки. Такому тесту область всё равно
приходится передавать: `scope` во всех читающих методах CRUD обязателен
намеренно, чтобы забытый фильтр падал, а не отдавал лишнее.
"""

from src.core.access import AccessScope, ScopeKind

#: Видно всё — как у админа.
ALL_SCOPE = AccessScope(
    kind=ScopeKind.ALL,
    user_id=0,
    division_ids=frozenset(),
    company_id=None,
)
