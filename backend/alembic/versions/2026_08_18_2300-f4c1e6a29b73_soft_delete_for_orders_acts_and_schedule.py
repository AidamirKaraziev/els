"""Мягкое удаление у заявок, актов и графика ТО

Офлайн-клиент не умеет узнавать об удалении: телефон спрашивает «что
изменилось с момента T», а удалённая строка на такой вопрос не отвечает
ничем — она просто перестаёт приходить и остаётся на телефоне навсегда.
Поэтому удаление становится архивированием: строка живёт дальше, но с
`is_actual=false`, и приходит в синхронизацию как признак «убрать».

Механизм в системе уже был — у объектов, компаний, организаций, участков,
договоров и контактных лиц. Здесь он доводится до тех сущностей, которые
уезжают на телефон механика.

Накопленные строки объявляются актуальными: до этой миграции архивных заявок
и актов не существовало, помечать в архив нечего.

Колонка `NOT NULL` со значением по умолчанию: `NULL` в трёхзначной логике
означал бы «неизвестно», и `WHERE is_actual = true` молча терял бы такие
строки из всех списков.

Индекса нет намеренно: значение почти всегда одно и то же, выборка по нему
всё равно пойдёт последовательным чтением, а место и стоимость записи индекс
занял бы.

Revision ID: f4c1e6a29b73
Revises: e3b9d47c58a2
Create Date: 2026-08-18 23:00:00.000000

"""

import sqlalchemy as sa
from alembic import op

revision = "f4c1e6a29b73"
down_revision = "e3b9d47c58a2"
branch_labels = None
depends_on = None

#: Всё, что уезжает на телефон механика: заявка, фактический акт и график ТО.
_TABLES = ("order", "acts_fact", "planned_to")


def upgrade():
    for table in _TABLES:
        op.add_column(
            table,
            sa.Column(
                "is_actual",
                sa.Boolean(),
                nullable=False,
                server_default=sa.true(),
            ),
        )


def downgrade():
    for table in _TABLES:
        op.drop_column(table, "is_actual")
