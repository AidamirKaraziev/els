import 'package:flutter/material.dart';

///Охрана труда

class WorksScreen extends StatelessWidget {
  const WorksScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [
          Center(child:  Text('Охрана труда')),
        ],
      ),
    );
  }
}